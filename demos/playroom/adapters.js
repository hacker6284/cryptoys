/**
 * Demo adapters — stubs.
 *
 * Thin shells that will only place toys on the felt and drive step state.
 * Crypto stays in the existing `demos/twodeck/` and `demos/scramble/` modules;
 * adapters never own the room.
 *
 * Deferred this PR: TwoDeck-in-room deal/layout, Scramble-on-table, fly-ins.
 * The teaching demos remain at ./twodeck/ and ./scramble/.
 */

export const adapters = {
    twodeck: null,
    scramble: null,
};
