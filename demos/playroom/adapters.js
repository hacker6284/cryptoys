/**
 * Demo adapters — stubs.
 *
 * Thin shells that will only place toys on the felt and drive step state.
 * Crypto stays in the existing `demos/doubledeal/` and `demos/scramble/` modules;
 * adapters never own the room.
 *
 * Deferred this PR: DoubleDeal-in-room deal/layout, Scramble-on-table, fly-ins.
 * The teaching demos remain at ./doubledeal/ and ./scramble/.
 */

export const adapters = {
    doubledeal: null,
    scramble: null,
};
