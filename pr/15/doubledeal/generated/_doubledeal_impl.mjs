// Generated by sudoc from doubledeal.sudo — do not edit; edit the sudo source.
import * as _rt from "./_sudo_rt.mjs";

export class Step {
    static _sudoKind = ["r", "Step"];
    static _sudoFields = ["kind", "label", "message", "key", "hand", "row", "col", "amount", "total", "card", "flag"];
    constructor(kind, label, message, key, hand, row, col, amount, total, card, flag) {
        this.kind = kind;
        this.label = label;
        this.message = message;
        this.key = key;
        this.hand = hand;
        this.row = row;
        this.col = col;
        this.amount = amount;
        this.total = total;
        this.card = card;
        this.flag = flag;
    }
}

export function suit_of(c) {
    return _rt.div(c, 13n);
}

export function rank_of(c) {
    return _rt.chk(_rt.mod_i64(c, 13n) + 1n);
}

export function left_rotate(xs, k) {
    let n = globalThis.BigInt(xs.length);
    if (n === 0n) {
        return _rt.dup(xs);
    }
    k = _rt.mod_i64(k, n);
    if (k === 0n) {
        return _rt.dup(xs);
    }
    let out = _rt.lst([]);
    {
        const _sudo_from_i = k;
        const _sudo_to_i = _rt.chk(n - 1n);
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            out.push(_rt.at(xs, i));
        }
    }
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = _rt.chk(k - 1n);
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            out.push(_rt.at(xs, i));
        }
    }
    return _rt.dup(out);
}

export function right_rotate(xs, k) {
    let n = globalThis.BigInt(xs.length);
    if (n === 0n) {
        return _rt.dup(xs);
    }
    k = _rt.mod_i64(k, n);
    if (k === 0n) {
        return _rt.dup(xs);
    }
    return left_rotate(xs, _rt.chk(n - k));
}

export function empty_rows() {
    let g = _rt.lst([]);
    {
        const _sudo_from_r = 0n;
        const _sudo_to_r = 3n;
        for (let r = _sudo_from_r; r <= _sudo_to_r; r += 1n) {
            let row = _rt.lst([]);
            {
                const _sudo_from_c = 0n;
                const _sudo_to_c = 12n;
                for (let c = _sudo_from_c; c <= _sudo_to_c; c += 1n) {
                    row.push(_rt.neg(1n));
                }
            }
            g.push(_rt.dup(row));
        }
    }
    return _rt.dup(g);
}

export function lay_cm(d) {
    let g = empty_rows();
    {
        const _sudo_from_k = 0n;
        const _sudo_to_k = 51n;
        for (let k = _sudo_from_k; k <= _sudo_to_k; k += 1n) {
            let r = _rt.mod_i64(k, 4n);
            let c = _rt.div(k, 4n);
            let row = _rt.dup(_rt.at(g, r));
            _rt.put(row, c, _rt.at(d, k));
            _rt.put(g, r, _rt.dup(row));
        }
    }
    return _rt.dup(g);
}

export function scoop_cm(g) {
    let out = _rt.lst([]);
    {
        const _sudo_from_c = 0n;
        const _sudo_to_c = 12n;
        for (let c = _sudo_from_c; c <= _sudo_to_c; c += 1n) {
            {
                const _sudo_from_r = 0n;
                const _sudo_to_r = 3n;
                for (let r = _sudo_from_r; r <= _sudo_to_r; r += 1n) {
                    out.push(_rt.at(_rt.at(g, r), c));
                }
            }
        }
    }
    return _rt.dup(out);
}

export function lay_rm(d) {
    let g = _rt.lst([]);
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = 3n;
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            let row = _rt.lst([]);
            {
                const _sudo_from_j = 0n;
                const _sudo_to_j = 12n;
                for (let j = _sudo_from_j; j <= _sudo_to_j; j += 1n) {
                    row.push(_rt.at(d, _rt.chk(_rt.chk(13n * i) + j)));
                }
            }
            g.push(_rt.dup(row));
        }
    }
    return _rt.dup(g);
}

export function scoop_rm(g) {
    let out = _rt.lst([]);
    {
        const _sudo_from_r = 0n;
        const _sudo_to_r = 3n;
        for (let r = _sudo_from_r; r <= _sudo_to_r; r += 1n) {
            {
                const _sudo_from_c = 0n;
                const _sudo_to_c = 12n;
                for (let c = _sudo_from_c; c <= _sudo_to_c; c += 1n) {
                    out.push(_rt.at(_rt.at(g, r), c));
                }
            }
        }
    }
    return _rt.dup(out);
}

export function sum_ranks(g) {
    g = _rt.dup(g);
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = 3n;
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            let row = _rt.dup(_rt.at(g, i));
            let total = 0n;
            {
                const _sudo_from_j = 0n;
                const _sudo_to_j = 12n;
                for (let j = _sudo_from_j; j <= _sudo_to_j; j += 1n) {
                    total = _rt.chk(total + rank_of(_rt.at(row, j)));
                }
            }
            _rt.put(g, i, left_rotate(row, _rt.mod_i64(total, 13n)));
        }
    }
    {
        const _sudo_from_j = 0n;
        const _sudo_to_j = 12n;
        for (let j = _sudo_from_j; j <= _sudo_to_j; j += 1n) {
            let col = _rt.lst([]);
            {
                const _sudo_from_i = 0n;
                const _sudo_to_i = 3n;
                for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
                    col.push(_rt.at(_rt.at(g, i), j));
                }
            }
            let total = 0n;
            {
                const _sudo_from_i = 0n;
                const _sudo_to_i = 3n;
                for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
                    total = _rt.chk(total + rank_of(_rt.at(col, i)));
                }
            }
            let s = _rt.mod_i64(total, 4n);
            let fresh = _rt.lst([]);
            {
                const _sudo_from_i = 0n;
                const _sudo_to_i = 3n;
                for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
                    fresh.push(_rt.at(col, _rt.mod_i64(_rt.chk(i - s), 4n)));
                }
            }
            {
                const _sudo_from_i = 0n;
                const _sudo_to_i = 3n;
                for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
                    let row = _rt.dup(_rt.at(g, i));
                    _rt.put(row, j, _rt.at(fresh, i));
                    _rt.put(g, i, _rt.dup(row));
                }
            }
        }
    }
    return _rt.dup(g);
}

export function inv_sum_ranks(g) {
    g = _rt.dup(g);
    {
        const _sudo_from_j = 0n;
        const _sudo_to_j = 12n;
        for (let j = _sudo_from_j; j <= _sudo_to_j; j += 1n) {
            let col = _rt.lst([]);
            {
                const _sudo_from_i = 0n;
                const _sudo_to_i = 3n;
                for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
                    col.push(_rt.at(_rt.at(g, i), j));
                }
            }
            let total = 0n;
            {
                const _sudo_from_i = 0n;
                const _sudo_to_i = 3n;
                for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
                    total = _rt.chk(total + rank_of(_rt.at(col, i)));
                }
            }
            let s = _rt.mod_i64(total, 4n);
            let fresh = _rt.lst([]);
            {
                const _sudo_from_i = 0n;
                const _sudo_to_i = 3n;
                for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
                    fresh.push(_rt.at(col, _rt.mod_i64(_rt.chk(i + s), 4n)));
                }
            }
            {
                const _sudo_from_i = 0n;
                const _sudo_to_i = 3n;
                for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
                    let row = _rt.dup(_rt.at(g, i));
                    _rt.put(row, j, _rt.at(fresh, i));
                    _rt.put(g, i, _rt.dup(row));
                }
            }
        }
    }
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = 3n;
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            let row = _rt.dup(_rt.at(g, i));
            let total = 0n;
            {
                const _sudo_from_j = 0n;
                const _sudo_to_j = 12n;
                for (let j = _sudo_from_j; j <= _sudo_to_j; j += 1n) {
                    total = _rt.chk(total + rank_of(_rt.at(row, j)));
                }
            }
            _rt.put(g, i, left_rotate(row, _rt.chk(0n - _rt.mod_i64(total, 13n))));
        }
    }
    return _rt.dup(g);
}

export function shift_rows(g) {
    g = _rt.dup(g);
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = 3n;
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            _rt.put(g, i, left_rotate(_rt.at(g, i), i));
        }
    }
    return _rt.dup(g);
}

export function inv_shift_rows(g) {
    g = _rt.dup(g);
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = 3n;
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            _rt.put(g, i, left_rotate(_rt.at(g, i), _rt.chk(0n - i)));
        }
    }
    return _rt.dup(g);
}

export function overflow_seat(occ, t) {
    {
        const _sudo_from_attempt = 0n;
        const _sudo_to_attempt = 3n;
        for (let attempt = _sudo_from_attempt; attempt <= _sudo_to_attempt; attempt += 1n) {
            let row = t;
            let found = _rt.neg(1n);
            {
                const _sudo_from_col = 0n;
                const _sudo_to_col = 12n;
                for (let col = _sudo_from_col; col <= _sudo_to_col; col += 1n) {
                    if (found < 0n && _rt.at(_rt.at(occ, row), col) === 0n) {
                        found = col;
                    }
                }
            }
            if (found >= 0n) {
                return [row, found, _rt.mod_i64(_rt.chk(t + 1n), 4n)];
            }
            t = _rt.mod_i64(_rt.chk(t + 1n), 4n);
        }
    }
    _rt.sudo_assert(false, 143);
    return [0n, 0n, 0n];
}

export function step_seat(card, r, c) {
    return [_rt.mod_i64(_rt.chk(r + suit_of(card)), 4n), _rt.mod_i64(_rt.chk(c + rank_of(card)), 13n)];
}

export function mix_columns(d) {
    let grid = empty_rows();
    let t = 0n;
    let prev_card = 0n;
    let prev_r = 0n;
    let prev_c = 0n;
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = 51n;
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            let card = _rt.at(d, i);
            let r = 2n;
            let c = 0n;
            if (i > 0n) {
                let tr;
                let tc;
                [tr, tc] = step_seat(prev_card, prev_r, prev_c);
                if (_rt.at(_rt.at(grid, tr), tc) < 0n) {
                    r = tr;
                    c = tc;
                } else {
                    let occ = _rt.lst([]);
                    {
                        const _sudo_from_rr = 0n;
                        const _sudo_to_rr = 3n;
                        for (let rr = _sudo_from_rr; rr <= _sudo_to_rr; rr += 1n) {
                            let marks = _rt.lst([]);
                            {
                                const _sudo_from_cc = 0n;
                                const _sudo_to_cc = 12n;
                                for (let cc = _sudo_from_cc; cc <= _sudo_to_cc; cc += 1n) {
                                    if (_rt.at(_rt.at(grid, rr), cc) < 0n) {
                                        marks.push(0n);
                                    } else {
                                        marks.push(1n);
                                    }
                                }
                            }
                            occ.push(_rt.dup(marks));
                        }
                    }
                    [r, c, t] = overflow_seat(occ, t);
                }
            }
            let row = _rt.dup(_rt.at(grid, r));
            _rt.put(row, c, card);
            _rt.put(grid, r, _rt.dup(row));
            prev_card = card;
            prev_r = r;
            prev_c = c;
        }
    }
    return scoop_rm(grid);
}

export function inv_mix_columns(d) {
    let grid = lay_rm(d);
    let visited = empty_rows();
    {
        const _sudo_from_r = 0n;
        const _sudo_to_r = 3n;
        for (let r = _sudo_from_r; r <= _sudo_to_r; r += 1n) {
            let row = _rt.dup(_rt.at(visited, r));
            {
                const _sudo_from_c = 0n;
                const _sudo_to_c = 12n;
                for (let c = _sudo_from_c; c <= _sudo_to_c; c += 1n) {
                    _rt.put(row, c, 0n);
                }
            }
            _rt.put(visited, r, _rt.dup(row));
        }
    }
    let t = 0n;
    let hand = _rt.lst([]);
    let prev_card = 0n;
    let prev_r = 0n;
    let prev_c = 0n;
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = 51n;
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            let r = 2n;
            let c = 0n;
            if (i > 0n) {
                let tr;
                let tc;
                [tr, tc] = step_seat(prev_card, prev_r, prev_c);
                if (_rt.at(_rt.at(visited, tr), tc) === 0n) {
                    r = tr;
                    c = tc;
                } else {
                    [r, c, t] = overflow_seat(visited, t);
                }
            }
            hand.push(_rt.at(_rt.at(grid, r), c));
            let row = _rt.dup(_rt.at(visited, r));
            _rt.put(row, c, 1n);
            _rt.put(visited, r, _rt.dup(row));
            prev_card = _rt.at(hand, _rt.chk(globalThis.BigInt(hand.length) - 1n));
            prev_r = r;
            prev_c = c;
        }
    }
    return _rt.dup(hand);
}

export function index_of(deck, card) {
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = 51n;
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            if (_rt.at(deck, i) === card) {
                return i;
            }
        }
    }
    _rt.sudo_assert(false, 219);
    return 0n;
}

export function compose(m, k) {
    let out = _rt.lst([]);
    {
        const _sudo_from_j = 0n;
        const _sudo_to_j = 51n;
        for (let j = _sudo_from_j; j <= _sudo_to_j; j += 1n) {
            out.push(_rt.at(m, index_of(k, j)));
        }
    }
    return _rt.dup(out);
}

export function inverse_compose(c, k) {
    let out = _rt.filled(52n, 0n);
    {
        const _sudo_from_j = 0n;
        const _sudo_to_j = 51n;
        for (let j = _sudo_from_j; j <= _sudo_to_j; j += 1n) {
            _rt.put(out, index_of(k, j), _rt.at(c, j));
        }
    }
    return _rt.dup(out);
}

export function push_front(xs, c) {
    let out = _rt.lst([]);
    out.push(c);
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = _rt.chk(globalThis.BigInt(xs.length) - 1n);
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            out.push(_rt.at(xs, i));
        }
    }
    return _rt.dup(out);
}

export function drop_front(xs) {
    let c = _rt.at(xs, 0n);
    let rest = _rt.lst([]);
    {
        const _sudo_from_i = 1n;
        const _sudo_to_i = _rt.chk(globalThis.BigInt(xs.length) - 1n);
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            rest.push(_rt.at(xs, i));
        }
    }
    return [c, _rt.dup(rest)];
}

export function passkey(deck) {
    let hand = _rt.dup(deck);
    let key = _rt.lst([]);
    let n = globalThis.BigInt(deck.length);
    {
        const _sudo_from_i = 1n;
        const _sudo_to_i = n;
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            let c;
            [c, hand] = drop_front(hand);
            if (globalThis.BigInt(hand.length) > 0n) {
                let k = _rt.mod_i64(suit_of(c), globalThis.BigInt(hand.length));
                if (k > 0n) {
                    hand = left_rotate(hand, k);
                }
            }
            if (globalThis.BigInt(hand.length) > 0n && rank_of(c) < globalThis.BigInt(hand.length)) {
                hand = left_rotate(hand, rank_of(c));
            } else if (globalThis.BigInt(key.length) > 0n && rank_of(c) < globalThis.BigInt(key.length)) {
                key = left_rotate(key, rank_of(c));
            }
            key = push_front(key, c);
        }
    }
    return _rt.dup(key);
}

export function passkey_inv(deck) {
    let key = _rt.dup(deck);
    let hand = _rt.lst([]);
    let n_deck = globalThis.BigInt(deck.length);
    {
        const _sudo_from_i = 1n;
        const _sudo_to_i = n_deck;
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            let c;
            [c, key] = drop_front(key);
            let n = globalThis.BigInt(hand.length);
            if (n > 0n && rank_of(c) < n) {
                hand = right_rotate(hand, rank_of(c));
            } else if (globalThis.BigInt(key.length) > 0n && rank_of(c) < globalThis.BigInt(key.length)) {
                key = right_rotate(key, rank_of(c));
            }
            if (n > 0n) {
                let k = _rt.mod_i64(suit_of(c), n);
                if (k > 0n) {
                    hand = right_rotate(hand, k);
                }
            }
            hand = push_front(hand, c);
        }
    }
    return _rt.dup(hand);
}

export function expand_keys(k0) {
    let keys = _rt.lst([]);
    keys.push(_rt.dup(k0));
    {
        const _sudo_from_r = 1n;
        const _sudo_to_r = 6n;
        for (let r = _sudo_from_r; r <= _sudo_to_r; r += 1n) {
            keys.push(passkey(_rt.at(keys, _rt.chk(r - 1n))));
        }
    }
    return _rt.dup(keys);
}

export function unkeyed_full(m) {
    let g = shift_rows(sum_ranks(lay_cm(m)));
    return mix_columns(scoop_cm(g));
}

export function full_round(m, kr) {
    return compose(unkeyed_full(m), kr);
}

export function inv_full_round(c, kr) {
    let m = inv_mix_columns(inverse_compose(c, kr));
    let g = inv_sum_ranks(inv_shift_rows(lay_cm(m)));
    return scoop_cm(g);
}

export function final_round(m, kr) {
    let g = shift_rows(sum_ranks(lay_cm(m)));
    return compose(scoop_cm(g), kr);
}

export function inv_final_round(c, kr) {
    let m = inverse_compose(c, kr);
    let g = inv_sum_ranks(inv_shift_rows(lay_cm(m)));
    return scoop_cm(g);
}

export function no_cards() {
    let empty = _rt.lst([]);
    return _rt.dup(empty);
}

export function add_step(steps, kind, label, message, key, hand, row, col, amount, total, card, flag) {
    steps = _rt.dup(steps);
    steps.push(_rt.rec(new Step(_rt.dup(kind), _rt.dup(label), _rt.dup(message), _rt.dup(key), _rt.dup(hand), row, col, amount, total, card, flag)));
    return _rt.dup(steps);
}

export function round_label(prefix, r) {
    let digits = _rt.txt("0123456789");
    let out = _rt.dup(prefix);
    out.push(_rt.at(digits, r));
    return _rt.dup(out);
}

export function trace_pass(deck, steps, label) {
    steps = _rt.dup(steps);
    let hand = _rt.dup(deck);
    let key = _rt.lst([]);
    let n = globalThis.BigInt(deck.length);
    {
        const _sudo_from_i = 1n;
        const _sudo_to_i = n;
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            let c;
            let next_hand;
            [c, next_hand] = drop_front(hand);
            let suit_cut = 0n;
            if (globalThis.BigInt(next_hand.length) > 0n) {
                suit_cut = _rt.mod_i64(suit_of(c), globalThis.BigInt(next_hand.length));
                if (suit_cut > 0n) {
                    next_hand = left_rotate(next_hand, suit_cut);
                }
            }
            let rank_cut = 0n;
            let cut_on = 0n;
            if (globalThis.BigInt(next_hand.length) > 0n && rank_of(c) < globalThis.BigInt(next_hand.length)) {
                rank_cut = rank_of(c);
                cut_on = 1n;
                next_hand = left_rotate(next_hand, rank_cut);
            } else if (globalThis.BigInt(key.length) > 0n && rank_of(c) < globalThis.BigInt(key.length)) {
                rank_cut = rank_of(c);
                cut_on = 2n;
                key = left_rotate(key, rank_cut);
            }
            key = push_front(key, c);
            hand = _rt.dup(next_hand);
            steps = add_step(steps, _rt.txt("pass"), label, no_cards(), key, hand, suit_of(c), rank_of(c), suit_cut, rank_cut, c, cut_on);
        }
    }
    return [_rt.dup(key), _rt.dup(steps)];
}

export function trace_unpass(deck, steps, label) {
    steps = _rt.dup(steps);
    let key = _rt.dup(deck);
    let hand = _rt.lst([]);
    let n_deck = globalThis.BigInt(deck.length);
    {
        const _sudo_from_i = 1n;
        const _sudo_to_i = n_deck;
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            let c;
            let next_key;
            [c, next_key] = drop_front(key);
            let n = globalThis.BigInt(hand.length);
            let rank_cut = 0n;
            let cut_on = 0n;
            if (n > 0n && rank_of(c) < n) {
                rank_cut = rank_of(c);
                cut_on = 1n;
                hand = right_rotate(hand, rank_cut);
            } else if (globalThis.BigInt(next_key.length) > 0n && rank_of(c) < globalThis.BigInt(next_key.length)) {
                rank_cut = rank_of(c);
                cut_on = 2n;
                next_key = right_rotate(next_key, rank_cut);
            }
            let suit_cut = 0n;
            if (n > 0n) {
                suit_cut = _rt.mod_i64(suit_of(c), n);
                if (suit_cut > 0n) {
                    hand = right_rotate(hand, suit_cut);
                }
            }
            hand = push_front(hand, c);
            key = _rt.dup(next_key);
            steps = add_step(steps, _rt.txt("unpass"), label, no_cards(), key, hand, suit_of(c), rank_of(c), suit_cut, rank_cut, c, cut_on);
        }
    }
    return [_rt.dup(hand), _rt.dup(steps)];
}

export function trace_sum(g, steps, label) {
    g = _rt.dup(g);
    steps = _rt.dup(steps);
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = 3n;
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            let row = _rt.dup(_rt.at(g, i));
            let total = 0n;
            {
                const _sudo_from_j = 0n;
                const _sudo_to_j = 12n;
                for (let j = _sudo_from_j; j <= _sudo_to_j; j += 1n) {
                    total = _rt.chk(total + rank_of(_rt.at(row, j)));
                }
            }
            let amount = _rt.mod_i64(total, 13n);
            _rt.put(g, i, left_rotate(row, amount));
            steps = add_step(steps, _rt.txt("sumrow"), label, no_cards(), no_cards(), no_cards(), i, _rt.neg(1n), amount, total, _rt.neg(1n), 0n);
        }
    }
    {
        const _sudo_from_j = 0n;
        const _sudo_to_j = 12n;
        for (let j = _sudo_from_j; j <= _sudo_to_j; j += 1n) {
            let col = _rt.lst([]);
            {
                const _sudo_from_i = 0n;
                const _sudo_to_i = 3n;
                for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
                    col.push(_rt.at(_rt.at(g, i), j));
                }
            }
            let total = 0n;
            {
                const _sudo_from_i = 0n;
                const _sudo_to_i = 3n;
                for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
                    total = _rt.chk(total + rank_of(_rt.at(col, i)));
                }
            }
            let amount = _rt.mod_i64(total, 4n);
            let fresh = _rt.lst([]);
            {
                const _sudo_from_i = 0n;
                const _sudo_to_i = 3n;
                for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
                    fresh.push(_rt.at(col, _rt.mod_i64(_rt.chk(i - amount), 4n)));
                }
            }
            {
                const _sudo_from_i = 0n;
                const _sudo_to_i = 3n;
                for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
                    let row = _rt.dup(_rt.at(g, i));
                    _rt.put(row, j, _rt.at(fresh, i));
                    _rt.put(g, i, _rt.dup(row));
                }
            }
            steps = add_step(steps, _rt.txt("sumcol"), label, no_cards(), no_cards(), no_cards(), _rt.neg(1n), j, amount, total, _rt.neg(1n), 0n);
        }
    }
    return [_rt.dup(g), _rt.dup(steps)];
}

export function trace_shift(g, steps, label) {
    g = _rt.dup(g);
    steps = _rt.dup(steps);
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = 3n;
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            _rt.put(g, i, left_rotate(_rt.at(g, i), i));
            steps = add_step(steps, _rt.txt("shift"), label, no_cards(), no_cards(), no_cards(), i, _rt.neg(1n), i, 0n, _rt.neg(1n), 0n);
        }
    }
    return [_rt.dup(g), _rt.dup(steps)];
}

export function trace_mix(d, steps, label) {
    steps = _rt.dup(steps);
    let grid = empty_rows();
    let t = 0n;
    let prev_card = 0n;
    let prev_r = 0n;
    let prev_c = 0n;
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = 51n;
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            let card = _rt.at(d, i);
            let r = 2n;
            let c = 0n;
            let overflowed = 0n;
            if (i === 0n) {
                steps = add_step(steps, _rt.txt("mark"), label, no_cards(), no_cards(), no_cards(), 2n, 0n, 0n, 0n, card, 0n);
            } else {
                let tr;
                let tc;
                [tr, tc] = step_seat(prev_card, prev_r, prev_c);
                if (_rt.at(_rt.at(grid, tr), tc) < 0n) {
                    r = tr;
                    c = tc;
                } else {
                    overflowed = 1n;
                    let occ = _rt.lst([]);
                    {
                        const _sudo_from_rr = 0n;
                        const _sudo_to_rr = 3n;
                        for (let rr = _sudo_from_rr; rr <= _sudo_to_rr; rr += 1n) {
                            let marks = _rt.lst([]);
                            {
                                const _sudo_from_cc = 0n;
                                const _sudo_to_cc = 12n;
                                for (let cc = _sudo_from_cc; cc <= _sudo_to_cc; cc += 1n) {
                                    if (_rt.at(_rt.at(grid, rr), cc) < 0n) {
                                        marks.push(0n);
                                    } else {
                                        marks.push(1n);
                                    }
                                }
                            }
                            occ.push(_rt.dup(marks));
                        }
                    }
                    let scan = t;
                    let found = _rt.neg(1n);
                    {
                        const _sudo_from_tries = 0n;
                        const _sudo_to_tries = 3n;
                        for (let tries = _sudo_from_tries; tries <= _sudo_to_tries; tries += 1n) {
                            if (found < 0n) {
                                steps = add_step(steps, _rt.txt("scan"), label, no_cards(), no_cards(), no_cards(), scan, _rt.neg(1n), 0n, 0n, card, 1n);
                                {
                                    const _sudo_from_col = 0n;
                                    const _sudo_to_col = 12n;
                                    for (let col = _sudo_from_col; col <= _sudo_to_col; col += 1n) {
                                        if (found < 0n && _rt.at(_rt.at(occ, scan), col) === 0n) {
                                            found = col;
                                        }
                                    }
                                }
                                if (found >= 0n) {
                                    r = scan;
                                    c = found;
                                    t = _rt.mod_i64(_rt.chk(scan + 1n), 4n);
                                } else {
                                    scan = _rt.mod_i64(_rt.chk(scan + 1n), 4n);
                                    t = scan;
                                }
                            }
                        }
                    }
                }
            }
            let row = _rt.dup(_rt.at(grid, r));
            _rt.put(row, c, card);
            _rt.put(grid, r, _rt.dup(row));
            steps = add_step(steps, _rt.txt("place"), label, no_cards(), no_cards(), no_cards(), r, c, 0n, 0n, card, overflowed);
            prev_card = card;
            prev_r = r;
            prev_c = c;
        }
    }
    return [scoop_rm(grid), _rt.dup(steps)];
}

export function trace_full_round(m, kr, steps, tag) {
    steps = _rt.dup(steps);
    steps = add_step(steps, _rt.txt("deal"), tag, m, kr, no_cards(), _rt.neg(1n), _rt.neg(1n), 0n, 0n, _rt.neg(1n), 0n);
    let g;
    [g, steps] = trace_sum(lay_cm(m), steps, tag);
    [g, steps] = trace_shift(g, steps, tag);
    let scooped = scoop_cm(g);
    steps = add_step(steps, _rt.txt("scoopcm"), tag, scooped, kr, no_cards(), _rt.neg(1n), _rt.neg(1n), 0n, 0n, _rt.neg(1n), 0n);
    let mixed;
    [mixed, steps] = trace_mix(scooped, steps, tag);
    steps = add_step(steps, _rt.txt("scooprm"), tag, mixed, kr, no_cards(), _rt.neg(1n), _rt.neg(1n), 0n, 0n, _rt.neg(1n), 0n);
    let out = compose(mixed, kr);
    steps = add_step(steps, _rt.txt("compose"), tag, mixed, kr, out, _rt.neg(1n), _rt.neg(1n), 0n, 0n, _rt.neg(1n), 0n);
    return [_rt.dup(out), _rt.dup(steps)];
}

export function trace_final_round(m, kr, steps) {
    steps = _rt.dup(steps);
    let tag = _rt.txt("Final round \u00b7 no GridCycle");
    steps = add_step(steps, _rt.txt("deal"), tag, m, kr, no_cards(), _rt.neg(1n), _rt.neg(1n), 0n, 0n, _rt.neg(1n), 1n);
    let g;
    [g, steps] = trace_sum(lay_cm(m), steps, tag);
    [g, steps] = trace_shift(g, steps, tag);
    let scooped = scoop_cm(g);
    steps = add_step(steps, _rt.txt("scoopcm"), tag, scooped, kr, no_cards(), _rt.neg(1n), _rt.neg(1n), 0n, 0n, _rt.neg(1n), 1n);
    let out = compose(scooped, kr);
    steps = add_step(steps, _rt.txt("compose"), _rt.txt("Final Compose \u00b7 ciphertext"), scooped, kr, out, _rt.neg(1n), _rt.neg(1n), 0n, 0n, _rt.neg(1n), 1n);
    return [_rt.dup(out), _rt.dup(steps)];
}

export function trace_encrypt(message, key) {
    let steps = _rt.lst([]);
    let whitened = compose(message, key);
    steps = add_step(steps, _rt.txt("compose"), _rt.txt("Whitening \u00b7 Compose \u00b7 AddRoundKey"), message, key, whitened, _rt.neg(1n), _rt.neg(1n), 0n, 0n, _rt.neg(1n), 0n);
    let m = _rt.dup(whitened);
    let built = _rt.dup(key);
    {
        const _sudo_from_r = 1n;
        const _sudo_to_r = 5n;
        for (let r = _sudo_from_r; r <= _sudo_to_r; r += 1n) {
            [built, steps] = trace_pass(built, steps, round_label(_rt.txt("PassKey K"), r));
            [m, steps] = trace_full_round(m, built, steps, round_label(_rt.txt("Round "), r));
        }
    }
    [built, steps] = trace_pass(built, steps, round_label(_rt.txt("PassKey K"), 6n));
    [m, steps] = trace_final_round(m, built, steps);
    return _rt.dup(steps);
}

export function trace_ecb(blocks, key) {
    let steps = _rt.lst([]);
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = _rt.chk(globalThis.BigInt(blocks.length) - 1n);
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            let one = trace_encrypt(_rt.at(blocks, i), key);
            {
                const _sudo_from_n = 0n;
                const _sudo_to_n = _rt.chk(globalThis.BigInt(one.length) - 1n);
                for (let n = _sudo_from_n; n <= _sudo_to_n; n += 1n) {
                    steps.push(_rt.dup(_rt.at(one, n)));
                }
            }
        }
    }
    return _rt.dup(steps);
}

export function trace_uncompose(c, kr, steps, label) {
    steps = _rt.dup(steps);
    let out = inverse_compose(c, kr);
    steps = add_step(steps, _rt.txt("uncompose"), label, c, kr, out, _rt.neg(1n), _rt.neg(1n), 0n, 0n, _rt.neg(1n), 0n);
    return [_rt.dup(out), _rt.dup(steps)];
}

export function trace_inv_shift(g, steps, label) {
    g = _rt.dup(g);
    steps = _rt.dup(steps);
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = 3n;
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            _rt.put(g, i, left_rotate(_rt.at(g, i), _rt.chk(0n - i)));
            steps = add_step(steps, _rt.txt("shift"), label, no_cards(), no_cards(), no_cards(), i, _rt.neg(1n), _rt.chk(0n - i), 0n, _rt.neg(1n), 0n);
        }
    }
    return [_rt.dup(g), _rt.dup(steps)];
}

export function trace_inv_sum(g, steps, label) {
    g = _rt.dup(g);
    steps = _rt.dup(steps);
    {
        const _sudo_from_j = 0n;
        const _sudo_to_j = 12n;
        for (let j = _sudo_from_j; j <= _sudo_to_j; j += 1n) {
            let col = _rt.lst([]);
            {
                const _sudo_from_i = 0n;
                const _sudo_to_i = 3n;
                for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
                    col.push(_rt.at(_rt.at(g, i), j));
                }
            }
            let total = 0n;
            {
                const _sudo_from_i = 0n;
                const _sudo_to_i = 3n;
                for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
                    total = _rt.chk(total + rank_of(_rt.at(col, i)));
                }
            }
            let s = _rt.mod_i64(total, 4n);
            let fresh = _rt.lst([]);
            {
                const _sudo_from_i = 0n;
                const _sudo_to_i = 3n;
                for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
                    fresh.push(_rt.at(col, _rt.mod_i64(_rt.chk(i + s), 4n)));
                }
            }
            {
                const _sudo_from_i = 0n;
                const _sudo_to_i = 3n;
                for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
                    let row = _rt.dup(_rt.at(g, i));
                    _rt.put(row, j, _rt.at(fresh, i));
                    _rt.put(g, i, _rt.dup(row));
                }
            }
            steps = add_step(steps, _rt.txt("sumcol"), label, no_cards(), no_cards(), no_cards(), _rt.neg(1n), j, _rt.chk(0n - s), total, _rt.neg(1n), 0n);
        }
    }
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = 3n;
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            let row = _rt.dup(_rt.at(g, i));
            let total = 0n;
            {
                const _sudo_from_j = 0n;
                const _sudo_to_j = 12n;
                for (let j = _sudo_from_j; j <= _sudo_to_j; j += 1n) {
                    total = _rt.chk(total + rank_of(_rt.at(row, j)));
                }
            }
            let amount = _rt.mod_i64(total, 13n);
            _rt.put(g, i, left_rotate(row, _rt.chk(0n - amount)));
            steps = add_step(steps, _rt.txt("sumrow"), label, no_cards(), no_cards(), no_cards(), i, _rt.neg(1n), _rt.chk(0n - amount), total, _rt.neg(1n), 0n);
        }
    }
    return [_rt.dup(g), _rt.dup(steps)];
}

export function trace_inv_mix(d, steps, label) {
    steps = _rt.dup(steps);
    steps = add_step(steps, _rt.txt("dealrm"), label, d, no_cards(), no_cards(), _rt.neg(1n), _rt.neg(1n), 0n, 0n, _rt.neg(1n), 0n);
    let grid = lay_rm(d);
    let visited = empty_rows();
    {
        const _sudo_from_r = 0n;
        const _sudo_to_r = 3n;
        for (let r = _sudo_from_r; r <= _sudo_to_r; r += 1n) {
            let row = _rt.dup(_rt.at(visited, r));
            {
                const _sudo_from_c = 0n;
                const _sudo_to_c = 12n;
                for (let c = _sudo_from_c; c <= _sudo_to_c; c += 1n) {
                    _rt.put(row, c, 0n);
                }
            }
            _rt.put(visited, r, _rt.dup(row));
        }
    }
    let t = 0n;
    let hand = _rt.lst([]);
    let prev_card = 0n;
    let prev_r = 0n;
    let prev_c = 0n;
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = 51n;
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            let r = 2n;
            let c = 0n;
            if (i > 0n) {
                let tr;
                let tc;
                [tr, tc] = step_seat(prev_card, prev_r, prev_c);
                if (_rt.at(_rt.at(visited, tr), tc) === 0n) {
                    r = tr;
                    c = tc;
                } else {
                    let scan = t;
                    let found = _rt.neg(1n);
                    {
                        const _sudo_from_tries = 0n;
                        const _sudo_to_tries = 3n;
                        for (let tries = _sudo_from_tries; tries <= _sudo_to_tries; tries += 1n) {
                            if (found < 0n) {
                                steps = add_step(steps, _rt.txt("scan"), label, no_cards(), no_cards(), no_cards(), scan, _rt.neg(1n), 0n, 0n, _rt.neg(1n), 1n);
                                {
                                    const _sudo_from_col = 0n;
                                    const _sudo_to_col = 12n;
                                    for (let col = _sudo_from_col; col <= _sudo_to_col; col += 1n) {
                                        if (found < 0n && _rt.at(_rt.at(visited, scan), col) === 0n) {
                                            found = col;
                                        }
                                    }
                                }
                                if (found >= 0n) {
                                    r = scan;
                                    c = found;
                                    t = _rt.mod_i64(_rt.chk(scan + 1n), 4n);
                                } else {
                                    scan = _rt.mod_i64(_rt.chk(scan + 1n), 4n);
                                    t = scan;
                                }
                            }
                        }
                    }
                }
            }
            let card = _rt.at(_rt.at(grid, r), c);
            steps = add_step(steps, _rt.txt("take"), label, no_cards(), no_cards(), no_cards(), r, c, i, 0n, card, 0n);
            hand.push(card);
            let row = _rt.dup(_rt.at(visited, r));
            _rt.put(row, c, 1n);
            _rt.put(visited, r, _rt.dup(row));
            prev_card = card;
            prev_r = r;
            prev_c = c;
        }
    }
    return [_rt.dup(hand), _rt.dup(steps)];
}

export function trace_inv_final(c, kr, steps, label) {
    steps = _rt.dup(steps);
    let m;
    [m, steps] = trace_uncompose(c, kr, steps, label);
    steps = add_step(steps, _rt.txt("deal"), label, m, kr, no_cards(), _rt.neg(1n), _rt.neg(1n), 0n, 0n, _rt.neg(1n), 1n);
    let g;
    [g, steps] = trace_inv_shift(lay_cm(m), steps, label);
    [g, steps] = trace_inv_sum(g, steps, label);
    let scooped = scoop_cm(g);
    steps = add_step(steps, _rt.txt("scoopcm"), label, scooped, kr, no_cards(), _rt.neg(1n), _rt.neg(1n), 0n, 0n, _rt.neg(1n), 1n);
    return [_rt.dup(scooped), _rt.dup(steps)];
}

export function trace_inv_full(c, kr, steps, label) {
    steps = _rt.dup(steps);
    let mixed;
    [mixed, steps] = trace_uncompose(c, kr, steps, label);
    let packet;
    [packet, steps] = trace_inv_mix(mixed, steps, label);
    steps = add_step(steps, _rt.txt("deal"), label, packet, kr, no_cards(), _rt.neg(1n), _rt.neg(1n), 0n, 0n, _rt.neg(1n), 0n);
    let g;
    [g, steps] = trace_inv_shift(lay_cm(packet), steps, label);
    [g, steps] = trace_inv_sum(g, steps, label);
    let scooped = scoop_cm(g);
    steps = add_step(steps, _rt.txt("scoopcm"), label, scooped, kr, no_cards(), _rt.neg(1n), _rt.neg(1n), 0n, 0n, _rt.neg(1n), 0n);
    return [_rt.dup(scooped), _rt.dup(steps)];
}

export function trace_decrypt(cipher, key) {
    let steps = _rt.lst([]);
    let built = _rt.dup(key);
    {
        const _sudo_from_r = 1n;
        const _sudo_to_r = 6n;
        for (let r = _sudo_from_r; r <= _sudo_to_r; r += 1n) {
            [built, steps] = trace_pass(built, steps, round_label(_rt.txt("PassKey K"), r));
        }
    }
    let m;
    [m, steps] = trace_inv_final(cipher, built, steps, _rt.txt("Inverse final round"));
    {
        const _sudo_from_r = 5n;
        const _sudo_to_r = 1n;
        for (let r = _sudo_from_r; r >= _sudo_to_r; r -= 1n) {
            [built, steps] = trace_unpass(built, steps, round_label(_rt.txt("Un-pass K"), r));
            [m, steps] = trace_inv_full(m, built, steps, round_label(_rt.txt("Inverse round "), r));
        }
    }
    [built, steps] = trace_unpass(built, steps, _rt.txt("Un-pass K0"));
    [m, steps] = trace_uncompose(m, built, steps, _rt.txt("InverseCompose \u00b7 K0"));
    return _rt.dup(steps);
}

export function trace_ctr(blocks, key, nonce) {
    let steps = _rt.lst([]);
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = _rt.chk(globalThis.BigInt(blocks.length) - 1n);
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            let counter = counter_deck(nonce, i);
            steps = add_step(steps, _rt.txt("counter"), _rt.txt("CTR counter"), counter, key, _rt.at(blocks, i), _rt.neg(1n), _rt.neg(1n), i, 0n, _rt.neg(1n), 0n);
            let stream = encrypt(counter, key);
            let walked = trace_encrypt(counter, key);
            {
                const _sudo_from_n = 0n;
                const _sudo_to_n = _rt.chk(globalThis.BigInt(walked.length) - 1n);
                for (let n = _sudo_from_n; n <= _sudo_to_n; n += 1n) {
                    steps.push(_rt.dup(_rt.at(walked, n)));
                }
            }
            let mixed = compose(_rt.at(blocks, i), stream);
            steps = add_step(steps, _rt.txt("compose"), _rt.txt("CTR Compose message with keystream"), _rt.at(blocks, i), stream, mixed, _rt.neg(1n), _rt.neg(1n), i, 0n, _rt.neg(1n), 0n);
        }
    }
    return _rt.dup(steps);
}

export function trace_ctr_decrypt(blocks, key, nonce) {
    let steps = _rt.lst([]);
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = _rt.chk(globalThis.BigInt(blocks.length) - 1n);
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            let counter = counter_deck(nonce, i);
            steps = add_step(steps, _rt.txt("counter"), _rt.txt("CTR counter"), counter, key, _rt.at(blocks, i), _rt.neg(1n), _rt.neg(1n), i, 0n, _rt.neg(1n), 0n);
            let stream = encrypt(counter, key);
            let walked = trace_encrypt(counter, key);
            {
                const _sudo_from_n = 0n;
                const _sudo_to_n = _rt.chk(globalThis.BigInt(walked.length) - 1n);
                for (let n = _sudo_from_n; n <= _sudo_to_n; n += 1n) {
                    steps.push(_rt.dup(_rt.at(walked, n)));
                }
            }
            let plain = inverse_compose(_rt.at(blocks, i), stream);
            steps = add_step(steps, _rt.txt("uncompose"), _rt.txt("CTR InverseCompose"), _rt.at(blocks, i), stream, plain, _rt.neg(1n), _rt.neg(1n), i, 0n, _rt.neg(1n), 0n);
        }
    }
    return _rt.dup(steps);
}

export function encrypt(message, key) {
    let keys = expand_keys(key);
    let m = compose(message, _rt.at(keys, 0n));
    {
        const _sudo_from_r = 1n;
        const _sudo_to_r = 5n;
        for (let r = _sudo_from_r; r <= _sudo_to_r; r += 1n) {
            m = full_round(m, _rt.at(keys, r));
        }
    }
    return final_round(m, _rt.at(keys, 6n));
}

export function decrypt(cipher, key) {
    let built = _rt.dup(key);
    {
        const _sudo_from_r = 1n;
        const _sudo_to_r = 6n;
        for (let r = _sudo_from_r; r <= _sudo_to_r; r += 1n) {
            built = passkey(built);
        }
    }
    let m = inv_final_round(cipher, built);
    {
        const _sudo_from_r = 5n;
        const _sudo_to_r = 1n;
        for (let r = _sudo_from_r; r >= _sudo_to_r; r -= 1n) {
            built = passkey_inv(built);
            m = inv_full_round(m, built);
        }
    }
    built = passkey_inv(built);
    return inverse_compose(m, built);
}

export function factorial(n) {
    let r = 1n;
    {
        const _sudo_from_i = 2n;
        const _sudo_to_i = n;
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            r = _rt.chk(r * i);
        }
    }
    return r;
}

export function unrank(items, rank) {
    items = _rt.dup(items);
    let n = globalThis.BigInt(items.length);
    rank = _rt.mod_i64(rank, factorial(n));
    let out = _rt.lst([]);
    {
        const _sudo_from_k = n;
        const _sudo_to_k = 1n;
        for (let k = _sudo_from_k; k >= _sudo_to_k; k -= 1n) {
            let f = factorial(_rt.chk(k - 1n));
            let idx = _rt.div(rank, f);
            rank = _rt.mod_i64(rank, f);
            out.push(_rt.at(items, idx));
            let rest = _rt.lst([]);
            {
                const _sudo_from_i = 0n;
                const _sudo_to_i = _rt.chk(globalThis.BigInt(items.length) - 1n);
                for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
                    if (i !== idx) {
                        rest.push(_rt.at(items, i));
                    }
                }
            }
            items = _rt.dup(rest);
        }
    }
    return _rt.dup(out);
}

export function diamond_cards() {
    let out = _rt.lst([]);
    {
        const _sudo_from_c = 39n;
        const _sudo_to_c = 51n;
        for (let c = _sudo_from_c; c <= _sudo_to_c; c += 1n) {
            out.push(c);
        }
    }
    return _rt.dup(out);
}

export function counter_deck(nonce, index) {
    _rt.sudo_assert_eq(globalThis.BigInt(nonce.length), 39n, 713);
    let diamonds = unrank(diamond_cards(), index);
    let out = _rt.lst([]);
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = 38n;
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            out.push(_rt.at(nonce, i));
        }
    }
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = 12n;
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            out.push(_rt.at(diamonds, i));
        }
    }
    return _rt.dup(out);
}

export function ecb_encrypt(blocks, key) {
    let out = _rt.lst([]);
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = _rt.chk(globalThis.BigInt(blocks.length) - 1n);
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            out.push(encrypt(_rt.at(blocks, i), key));
        }
    }
    return _rt.dup(out);
}

export function ecb_decrypt(blocks, key) {
    let out = _rt.lst([]);
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = _rt.chk(globalThis.BigInt(blocks.length) - 1n);
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            out.push(decrypt(_rt.at(blocks, i), key));
        }
    }
    return _rt.dup(out);
}

export function ctr_encrypt(blocks, key, nonce) {
    let out = _rt.lst([]);
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = _rt.chk(globalThis.BigInt(blocks.length) - 1n);
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            let stream = encrypt(counter_deck(nonce, i), key);
            out.push(compose(_rt.at(blocks, i), stream));
        }
    }
    return _rt.dup(out);
}

export function ctr_decrypt(blocks, key, nonce) {
    let out = _rt.lst([]);
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = _rt.chk(globalThis.BigInt(blocks.length) - 1n);
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            let stream = encrypt(counter_deck(nonce, i), key);
            out.push(inverse_compose(_rt.at(blocks, i), stream));
        }
    }
    return _rt.dup(out);
}

export function sorted_copy(xs) {
    let out = _rt.dup(xs);
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = _rt.chk(globalThis.BigInt(out.length) - 1n);
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            {
                const _sudo_from_j = 0n;
                const _sudo_to_j = _rt.chk(globalThis.BigInt(out.length) - 2n);
                for (let j = _sudo_from_j; j <= _sudo_to_j; j += 1n) {
                    if (_rt.at(out, j) > _rt.at(out, _rt.chk(j + 1n))) {
                        let a = _rt.at(out, j);
                        _rt.put(out, j, _rt.at(out, _rt.chk(j + 1n)));
                        _rt.put(out, _rt.chk(j + 1n), a);
                    }
                }
            }
        }
    }
    return _rt.dup(out);
}

export function same_cards(a, b) {
    return _rt.eq(sorted_copy(a), sorted_copy(b));
}

