// Generated by sudoc from scramble.sudo — do not edit; edit the sudo source.
import * as _rt from "./_sudo_rt.mjs";

export class Cubie {
    static _sudoKind = ["r", "Cubie"];
    static _sudoFields = ["x", "y", "z", "xp", "xn", "yp", "yn", "zp", "zn"];
    constructor(x, y, z, xp, xn, yp, yn, zp, zn) {
        this.x = x;
        this.y = y;
        this.z = z;
        this.xp = xp;
        this.xn = xn;
        this.yp = yp;
        this.yn = yn;
        this.zp = zp;
        this.zn = zn;
    }
}

export class Step {
    static _sudoKind = ["r", "Step"];
    static _sudoFields = ["kind", "move", "nybble", "block", "index", "up", "front", "facelets"];
    constructor(kind, move, nybble, block, index, up, front, facelets) {
        this.kind = kind;
        this.move = move;
        this.nybble = nybble;
        this.block = block;
        this.index = index;
        this.up = up;
        this.front = front;
        this.facelets = facelets;
    }
}

export class Evaluation {
    static _sudoKind = ["r", "Evaluation"];
    static _sudoFields = ["digest", "trace"];
    constructor(digest, trace) {
        this.digest = digest;
        this.trace = trace;
    }
}

export class Scramble {
    static _sudoKind = ["r", "Scramble"];
    static _sudoFields = ["version", "message", "processed", "cube", "steps", "done"];
    constructor(version, message, processed, cube, steps, done) {
        this.version = version;
        this.message = message;
        this.processed = processed;
        this.cube = cube;
        this.steps = steps;
        this.done = done;
    }
}

export const v1_face = _rt.lst([0n, 0n, 1n, 1n, 3n, 3n, 2n, 2n, 4n, 4n, 5n, 5n, 0n, 1n, 3n, 2n]);
export const v1_turns = _rt.lst([1n, 3n, 1n, 3n, 1n, 3n, 1n, 3n, 1n, 3n, 1n, 3n, 2n, 2n, 2n, 2n]);
export const v2_a = _rt.lst([0n, 0n, 0n, 0n, 1n, 1n, 2n, 2n, 4n, 4n, 5n, 4n, 3n, 4n, 2n, 2n]);
export const v2_b = _rt.lst([2n, 4n, 3n, 5n, 2n, 4n, 0n, 1n, 0n, 1n, 0n, 2n, 0n, 3n, 4n, 5n]);
export const tape_f = _rt.lst([6n, 0n, 7n, 1n, 8n, 2n, 9n, 3n]);
export const tape_i = _rt.lst([6n, 0n, 7n, 1n]);
export const fx = _rt.lst([-1n, 0n, 1n, -1n, 0n, 1n, -1n, 0n, 1n, 1n, 1n, 1n, 1n, 1n, 1n, 1n, 1n, 1n, -1n, 0n, 1n, -1n, 0n, 1n, -1n, 0n, 1n, -1n, 0n, 1n, -1n, 0n, 1n, -1n, 0n, 1n, -1n, -1n, -1n, -1n, -1n, -1n, -1n, -1n, -1n, 1n, 0n, -1n, 1n, 0n, -1n, 1n, 0n, -1n]);
export const fy = _rt.lst([1n, 1n, 1n, 1n, 1n, 1n, 1n, 1n, 1n, 1n, 1n, 1n, 0n, 0n, 0n, -1n, -1n, -1n, 1n, 1n, 1n, 0n, 0n, 0n, -1n, -1n, -1n, -1n, -1n, -1n, -1n, -1n, -1n, -1n, -1n, -1n, 1n, 1n, 1n, 0n, 0n, 0n, -1n, -1n, -1n, 1n, 1n, 1n, 0n, 0n, 0n, -1n, -1n, -1n]);
export const fz = _rt.lst([-1n, -1n, -1n, 0n, 0n, 0n, 1n, 1n, 1n, 1n, 0n, -1n, 1n, 0n, -1n, 1n, 0n, -1n, 1n, 1n, 1n, 1n, 1n, 1n, 1n, 1n, 1n, 1n, 1n, 1n, 0n, 0n, 0n, -1n, -1n, -1n, -1n, 0n, 1n, -1n, 0n, 1n, -1n, 0n, 1n, -1n, -1n, -1n, -1n, -1n, -1n, -1n, -1n, -1n]);
export const fa = _rt.lst([2n, 2n, 2n, 2n, 2n, 2n, 2n, 2n, 2n, 0n, 0n, 0n, 0n, 0n, 0n, 0n, 0n, 0n, 4n, 4n, 4n, 4n, 4n, 4n, 4n, 4n, 4n, 3n, 3n, 3n, 3n, 3n, 3n, 3n, 3n, 3n, 1n, 1n, 1n, 1n, 1n, 1n, 1n, 1n, 1n, 5n, 5n, 5n, 5n, 5n, 5n, 5n, 5n, 5n]);
export const cx = _rt.lst([1n, -1n, -1n, 1n, 1n, -1n, -1n, 1n]);
export const cy = _rt.lst([1n, 1n, 1n, 1n, -1n, -1n, -1n, -1n]);
export const cz = _rt.lst([1n, 1n, -1n, -1n, 1n, 1n, -1n, -1n]);
export const cax = _rt.lst([2n, 0n, 4n, 2n, 4n, 1n, 2n, 1n, 5n, 2n, 5n, 0n, 3n, 4n, 0n, 3n, 1n, 4n, 3n, 5n, 1n, 3n, 0n, 5n]);
export const ex = _rt.lst([1n, 0n, -1n, 0n, 1n, 0n, -1n, 0n, 1n, -1n, -1n, 1n]);
export const ey = _rt.lst([1n, 1n, 1n, 1n, -1n, -1n, -1n, -1n, 0n, 0n, 0n, 0n]);
export const ez = _rt.lst([0n, 1n, 0n, -1n, 0n, 1n, 0n, -1n, 1n, 1n, -1n, -1n]);
export const eax = _rt.lst([2n, 0n, 2n, 4n, 2n, 1n, 2n, 5n, 3n, 0n, 3n, 4n, 3n, 1n, 3n, 5n, 4n, 0n, 4n, 1n, 5n, 1n, 5n, 0n]);

export function solved_cube() {
    let cube = _rt.lst([]);
    {
        const _sudo_from_x = _rt.neg(1n);
        const _sudo_to_x = 1n;
        for (let x = _sudo_from_x; x <= _sudo_to_x; x += 1n) {
            {
                const _sudo_from_y = _rt.neg(1n);
                const _sudo_to_y = 1n;
                for (let y = _sudo_from_y; y <= _sudo_to_y; y += 1n) {
                    {
                        const _sudo_from_z = _rt.neg(1n);
                        const _sudo_to_z = 1n;
                        for (let z = _sudo_from_z; z <= _sudo_to_z; z += 1n) {
                            let core = x === 0n && y === 0n && z === 0n;
                            if (!core) {
                                let xp = 0n;
                                let xn = 0n;
                                let yp = 0n;
                                let yn = 0n;
                                let zp = 0n;
                                let zn = 0n;
                                if (y === 1n) {
                                    yp = 1n;
                                }
                                if (y === _rt.neg(1n)) {
                                    yn = 2n;
                                }
                                if (x === 1n) {
                                    xp = 3n;
                                }
                                if (x === _rt.neg(1n)) {
                                    xn = 4n;
                                }
                                if (z === 1n) {
                                    zp = 6n;
                                }
                                if (z === _rt.neg(1n)) {
                                    zn = 5n;
                                }
                                cube.push(_rt.rec(new Cubie(x, y, z, xp, xn, yp, yn, zp, zn)));
                            }
                        }
                    }
                }
            }
        }
    }
    return _rt.dup(cube);
}

export function fresh(version) {
    let msg = _rt.lst([]);
    let steps = _rt.lst([]);
    return _rt.rec(new Scramble(version, _rt.dup(msg), 0n, solved_cube(), _rt.dup(steps), false));
}

export function scramble_v1() {
    return fresh(1n);
}

export function scramble_v2() {
    return fresh(2n);
}

export function rot_xyz(face, x, y, z) {
    if (face === 0n || face === 1n) {
        return [z, y, _rt.chk(0n - x)];
    }
    if (face === 2n) {
        return [x, z, _rt.chk(0n - y)];
    }
    if (face === 3n) {
        return [x, _rt.chk(0n - z), y];
    }
    if (face === 4n) {
        return [y, _rt.chk(0n - x), z];
    }
    return [_rt.chk(0n - y), x, z];
}

export function on_face(face, x, y, z) {
    if (face === 0n) {
        return y === 1n;
    }
    if (face === 1n) {
        return y === _rt.neg(1n);
    }
    if (face === 2n) {
        return x === 1n;
    }
    if (face === 3n) {
        return x === _rt.neg(1n);
    }
    if (face === 4n) {
        return z === 1n;
    }
    return z === _rt.neg(1n);
}

export function write_axis(xp, xn, yp, yn, zp, zn, x, y, z, color) {
    if (x === 1n && y === 0n && z === 0n) {
        return [color, xn, yp, yn, zp, zn];
    }
    if (x === _rt.neg(1n) && y === 0n && z === 0n) {
        return [xp, color, yp, yn, zp, zn];
    }
    if (x === 0n && y === 1n && z === 0n) {
        return [xp, xn, color, yn, zp, zn];
    }
    if (x === 0n && y === _rt.neg(1n) && z === 0n) {
        return [xp, xn, yp, color, zp, zn];
    }
    if (x === 0n && y === 0n && z === 1n) {
        return [xp, xn, yp, yn, color, zn];
    }
    return [xp, xn, yp, yn, zp, color];
}

export function turn_cubie(c, face) {
    let nx;
    let ny;
    let nz;
    [nx, ny, nz] = rot_xyz(face, c.x, c.y, c.z);
    let xp = 0n;
    let xn = 0n;
    let yp = 0n;
    let yn = 0n;
    let zp = 0n;
    let zn = 0n;
    if (c.xp !== 0n) {
        let rx;
        let ry;
        let rz;
        [rx, ry, rz] = rot_xyz(face, 1n, 0n, 0n);
        [xp, xn, yp, yn, zp, zn] = write_axis(xp, xn, yp, yn, zp, zn, rx, ry, rz, c.xp);
    }
    if (c.xn !== 0n) {
        let rx;
        let ry;
        let rz;
        [rx, ry, rz] = rot_xyz(face, _rt.neg(1n), 0n, 0n);
        [xp, xn, yp, yn, zp, zn] = write_axis(xp, xn, yp, yn, zp, zn, rx, ry, rz, c.xn);
    }
    if (c.yp !== 0n) {
        let rx;
        let ry;
        let rz;
        [rx, ry, rz] = rot_xyz(face, 0n, 1n, 0n);
        [xp, xn, yp, yn, zp, zn] = write_axis(xp, xn, yp, yn, zp, zn, rx, ry, rz, c.yp);
    }
    if (c.yn !== 0n) {
        let rx;
        let ry;
        let rz;
        [rx, ry, rz] = rot_xyz(face, 0n, _rt.neg(1n), 0n);
        [xp, xn, yp, yn, zp, zn] = write_axis(xp, xn, yp, yn, zp, zn, rx, ry, rz, c.yn);
    }
    if (c.zp !== 0n) {
        let rx;
        let ry;
        let rz;
        [rx, ry, rz] = rot_xyz(face, 0n, 0n, 1n);
        [xp, xn, yp, yn, zp, zn] = write_axis(xp, xn, yp, yn, zp, zn, rx, ry, rz, c.zp);
    }
    if (c.zn !== 0n) {
        let rx;
        let ry;
        let rz;
        [rx, ry, rz] = rot_xyz(face, 0n, 0n, _rt.neg(1n));
        [xp, xn, yp, yn, zp, zn] = write_axis(xp, xn, yp, yn, zp, zn, rx, ry, rz, c.zn);
    }
    return _rt.rec(new Cubie(nx, ny, nz, xp, xn, yp, yn, zp, zn));
}

export function quarter(cube, face) {
    let out = _rt.lst([]);
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = _rt.chk(globalThis.BigInt(cube.length) - 1n);
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            let c = _rt.dup(_rt.at(cube, i));
            if (on_face(face, c.x, c.y, c.z)) {
                out.push(turn_cubie(c, face));
            } else {
                out.push(_rt.dup(c));
            }
        }
    }
    return _rt.dup(out);
}

export function apply_turns(cube, face, turns) {
    let out = _rt.dup(cube);
    {
        const _sudo_from_i = 1n;
        const _sudo_to_i = turns;
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            out = quarter(out, face);
        }
    }
    return _rt.dup(out);
}

export function cubie_at(cube, x, y, z) {
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = _rt.chk(globalThis.BigInt(cube.length) - 1n);
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            let c = _rt.dup(_rt.at(cube, i));
            if (c.x === x && c.y === y && c.z === z) {
                return i;
            }
        }
    }
    _rt.sudo_assert(false, 196);
    return 0n;
}

export function sticker_on(c, axis) {
    if (axis === 0n) {
        return c.xp;
    }
    if (axis === 1n) {
        return c.xn;
    }
    if (axis === 2n) {
        return c.yp;
    }
    if (axis === 3n) {
        return c.yn;
    }
    if (axis === 4n) {
        return c.zp;
    }
    return c.zn;
}

export function color_char(color) {
    if (color === 1n) {
        return 87n;
    }
    if (color === 2n) {
        return 89n;
    }
    if (color === 3n) {
        return 82n;
    }
    if (color === 4n) {
        return 79n;
    }
    if (color === 5n) {
        return 66n;
    }
    if (color === 6n) {
        return 71n;
    }
    _rt.sudo_assert(false, 225);
    return 63n;
}

export function facelets_of(cube) {
    let out = _rt.txt("");
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = 53n;
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            let c = _rt.dup(_rt.at(cube, cubie_at(cube, _rt.at(fx, i), _rt.at(fy, i), _rt.at(fz, i))));
            out.push(color_char(sticker_on(c, _rt.at(fa, i))));
        }
    }
    return _rt.dup(out);
}

export function solved_facelets() {
    return facelets_of(solved_cube());
}

export function letter(color) {
    let out = _rt.txt("");
    out.push(color_char(color));
    return _rt.dup(out);
}

export function hex_digit(n) {
    let digits = _rt.txt("0123456789ABCDEF");
    let out = _rt.txt("");
    out.push(_rt.at(digits, n));
    return _rt.dup(out);
}

export function move_name(face, turns) {
    let names = _rt.txt("UDRLFB");
    let out = _rt.txt("");
    out.push(_rt.at(names, face));
    if (turns === 2n) {
        out.push(50n);
    }
    if (turns === 3n) {
        out.push(39n);
    }
    return _rt.dup(out);
}

export function push_step(s, kind, move, nybble, block, at, up, front) {
    let steps = _rt.dup(s.steps);
    steps.push(_rt.rec(new Step(_rt.dup(kind), _rt.dup(move), _rt.dup(nybble), block, at, _rt.dup(up), _rt.dup(front), facelets_of(s.cube))));
    s.steps = _rt.dup(steps);
    return s;
}

export function blank() {
    return _rt.txt("");
}

export function is_center(c) {
    let zeros = 0n;
    if (c.x === 0n) {
        zeros = _rt.chk(zeros + 1n);
    }
    if (c.y === 0n) {
        zeros = _rt.chk(zeros + 1n);
    }
    if (c.z === 0n) {
        zeros = _rt.chk(zeros + 1n);
    }
    return zeros === 2n;
}

export function has_color(c, color) {
    if (c.xp === color) {
        return true;
    }
    if (c.xn === color) {
        return true;
    }
    if (c.yp === color) {
        return true;
    }
    if (c.yn === color) {
        return true;
    }
    if (c.zp === color) {
        return true;
    }
    return c.zn === color;
}

export function center_dir(cube, color) {
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = _rt.chk(globalThis.BigInt(cube.length) - 1n);
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            let c = _rt.dup(_rt.at(cube, i));
            if (is_center(c) && has_color(c, color)) {
                return [c.x, c.y, c.z];
            }
        }
    }
    _rt.sudo_assert(false, 295);
    return [0n, 0n, 0n];
}

export function cross(ax, ay, az, bx, by, bz) {
    return [_rt.chk(_rt.chk(ay * bz) - _rt.chk(az * by)), _rt.chk(_rt.chk(az * bx) - _rt.chk(ax * bz)), _rt.chk(_rt.chk(ax * by) - _rt.chk(ay * bx))];
}

export function mul_vec(m, x, y, z) {
    let nx = _rt.chk(_rt.chk(_rt.chk(_rt.at(_rt.at(m, 0n), 0n) * x) + _rt.chk(_rt.at(_rt.at(m, 0n), 1n) * y)) + _rt.chk(_rt.at(_rt.at(m, 0n), 2n) * z));
    let ny = _rt.chk(_rt.chk(_rt.chk(_rt.at(_rt.at(m, 1n), 0n) * x) + _rt.chk(_rt.at(_rt.at(m, 1n), 1n) * y)) + _rt.chk(_rt.at(_rt.at(m, 1n), 2n) * z));
    let nz = _rt.chk(_rt.chk(_rt.chk(_rt.at(_rt.at(m, 2n), 0n) * x) + _rt.chk(_rt.at(_rt.at(m, 2n), 1n) * y)) + _rt.chk(_rt.at(_rt.at(m, 2n), 2n) * z));
    return [nx, ny, nz];
}

export function paint_cubie(x, y, z, m, xp, xn, yp, yn, zp, zn, ax, ay, az, color) {
    if (color === 0n) {
        return [xp, xn, yp, yn, zp, zn];
    }
    let rx;
    let ry;
    let rz;
    [rx, ry, rz] = mul_vec(m, ax, ay, az);
    return write_axis(xp, xn, yp, yn, zp, zn, rx, ry, rz, color);
}

export function apply_matrix(cube, m) {
    let out = _rt.lst([]);
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = _rt.chk(globalThis.BigInt(cube.length) - 1n);
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            let c = _rt.dup(_rt.at(cube, i));
            let nx;
            let ny;
            let nz;
            [nx, ny, nz] = mul_vec(m, c.x, c.y, c.z);
            let xp = 0n;
            let xn = 0n;
            let yp = 0n;
            let yn = 0n;
            let zp = 0n;
            let zn = 0n;
            [xp, xn, yp, yn, zp, zn] = paint_cubie(nx, ny, nz, m, xp, xn, yp, yn, zp, zn, 1n, 0n, 0n, c.xp);
            [xp, xn, yp, yn, zp, zn] = paint_cubie(nx, ny, nz, m, xp, xn, yp, yn, zp, zn, _rt.neg(1n), 0n, 0n, c.xn);
            [xp, xn, yp, yn, zp, zn] = paint_cubie(nx, ny, nz, m, xp, xn, yp, yn, zp, zn, 0n, 1n, 0n, c.yp);
            [xp, xn, yp, yn, zp, zn] = paint_cubie(nx, ny, nz, m, xp, xn, yp, yn, zp, zn, 0n, _rt.neg(1n), 0n, c.yn);
            [xp, xn, yp, yn, zp, zn] = paint_cubie(nx, ny, nz, m, xp, xn, yp, yn, zp, zn, 0n, 0n, 1n, c.zp);
            [xp, xn, yp, yn, zp, zn] = paint_cubie(nx, ny, nz, m, xp, xn, yp, yn, zp, zn, 0n, 0n, _rt.neg(1n), c.zn);
            out.push(_rt.rec(new Cubie(nx, ny, nz, xp, xn, yp, yn, zp, zn)));
        }
    }
    return _rt.dup(out);
}

export function reorient(cube, up_color, front_color) {
    let ux;
    let uy;
    let uz;
    [ux, uy, uz] = center_dir(cube, up_color);
    let px;
    let py;
    let pz;
    [px, py, pz] = center_dir(cube, front_color);
    let seated = ux === 0n && uy === 1n && uz === 0n && px === 0n && py === 0n && pz === 1n;
    if (seated) {
        return _rt.dup(cube);
    }
    let vx;
    let vy;
    let vz;
    [vx, vy, vz] = cross(ux, uy, uz, px, py, pz);
    let m = _rt.lst([]);
    m.push(_rt.lst([vx, vy, vz]));
    m.push(_rt.lst([ux, uy, uz]));
    m.push(_rt.lst([px, py, pz]));
    return apply_matrix(cube, m);
}

export function do_move(s, face, turns, nybble, block, at) {
    s.cube = apply_turns(s.cube, face, turns);
    s = push_step(s, _rt.txt("move"), move_name(face, turns), nybble, block, at, blank(), blank());
    return s;
}

export function do_rule(s, block) {
    let c = _rt.dup(_rt.at(s.cube, cubie_at(s.cube, 1n, 1n, 1n)));
    let up = c.yp;
    let front = c.zp;
    s.cube = reorient(s.cube, up, front);
    s = push_step(s, _rt.txt("ruleB"), blank(), blank(), block, 0n, letter(up), letter(front));
    return s;
}

export function apply_v1_block(s, ny, block) {
    let base = _rt.chk(block * 8n);
    {
        const _sudo_from_k = 0n;
        const _sudo_to_k = 7n;
        for (let k = _sudo_from_k; k <= _sudo_to_k; k += 1n) {
            let n = _rt.at(ny, _rt.chk(base + k));
            s = do_move(s, _rt.at(v1_face, n), _rt.at(v1_turns, n), hex_digit(n), block, k);
        }
    }
    s = do_rule(s, block);
    return s;
}

export function apply_v2_symbol(s, n, block) {
    let shown = hex_digit(n);
    s = do_move(s, _rt.at(v2_a, n), 1n, shown, block, 0n);
    s = do_move(s, _rt.at(v2_b, n), 1n, shown, block, 1n);
    s = do_rule(s, block);
    return s;
}

export function nybbles_of(message) {
    let out = _rt.lst([]);
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = _rt.chk(globalThis.BigInt(message.length) - 1n);
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            let b = _rt.at(message, i);
            out.push(_rt.div(b, 16n));
            out.push(_rt.mod_i64(b, 16n));
        }
    }
    return _rt.dup(out);
}

export function pad_tape(ny, version) {
    let out = _rt.lst([]);
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = _rt.chk(globalThis.BigInt(ny.length) - 1n);
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            out.push(_rt.at(ny, i));
        }
    }
    out.push(8n);
    if (version === 1n) {
        let n = _rt.mod_i64(_rt.chk(8n - _rt.mod_i64(globalThis.BigInt(out.length), 8n)), 8n);
        {
            const _sudo_from_i = 0n;
            const _sudo_to_i = _rt.chk(n - 1n);
            for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
                out.push(_rt.at(tape_f, i));
            }
        }
        let remaining = _rt.chk(24n - globalThis.BigInt(out.length));
        {
            const _sudo_from_i = 0n;
            const _sudo_to_i = _rt.chk(remaining - 1n);
            for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
                out.push(_rt.at(tape_f, _rt.mod_i64(i, 8n)));
            }
        }
        return _rt.dup(out);
    }
    let remaining = _rt.chk(12n - globalThis.BigInt(out.length));
    {
        const _sudo_from_k = 0n;
        const _sudo_to_k = _rt.chk(remaining - 1n);
        for (let k = _sudo_from_k; k <= _sudo_to_k; k += 1n) {
            out.push(_rt.at(tape_i, _rt.mod_i64(k, 4n)));
        }
    }
    return _rt.dup(out);
}

export function apply_ready(s) {
    let ny = nybbles_of(s.message);
    if (s.version === 1n) {
        let nready = _rt.div(_rt.chk(globalThis.BigInt(ny.length) - s.processed), 8n);
        {
            const _sudo_from_b = 0n;
            const _sudo_to_b = _rt.chk(nready - 1n);
            for (let b = _sudo_from_b; b <= _sudo_to_b; b += 1n) {
                s = apply_v1_block(s, ny, _rt.div(s.processed, 8n));
                s.processed = _rt.chk(s.processed + 8n);
            }
        }
    } else {
        let nleft = _rt.chk(globalThis.BigInt(ny.length) - s.processed);
        {
            const _sudo_from_k = 0n;
            const _sudo_to_k = _rt.chk(nleft - 1n);
            for (let k = _sudo_from_k; k <= _sudo_to_k; k += 1n) {
                s = apply_v2_symbol(s, _rt.at(ny, s.processed), s.processed);
                s.processed = _rt.chk(s.processed + 1n);
            }
        }
    }
    return s;
}

export function update(s, message) {
    _rt.sudo_assert(!s.done, 411);
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = _rt.chk(globalThis.BigInt(message.length) - 1n);
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            let b = _rt.at(message, i);
            _rt.sudo_assert(b >= 0n && b <= 255n, 414);
        }
    }
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = _rt.chk(globalThis.BigInt(message.length) - 1n);
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            _rt.field_mut(s, "message").push(_rt.at(message, i));
        }
    }
    s = apply_ready(s);
    return s;
}

export function fact(n) {
    let r = 1n;
    {
        const _sudo_from_i = 2n;
        const _sudo_to_i = n;
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            r = _rt.chk(r * i);
        }
    }
    return r;
}

export function rank_perm(p) {
    let total = 0n;
    {
        const _sudo_from_n = 0n;
        const _sudo_to_n = _rt.chk(globalThis.BigInt(p.length) - 1n);
        for (let n = _sudo_from_n; n <= _sudo_to_n; n += 1n) {
            let inv = 0n;
            {
                const _sudo_from_j = _rt.chk(n + 1n);
                const _sudo_to_j = _rt.chk(globalThis.BigInt(p.length) - 1n);
                for (let j = _sudo_from_j; j <= _sudo_to_j; j += 1n) {
                    if (_rt.at(p, j) < _rt.at(p, n)) {
                        inv = _rt.chk(inv + 1n);
                    }
                }
            }
            total = _rt.chk(total + _rt.chk(inv * fact(_rt.chk(_rt.chk(globalThis.BigInt(p.length) - 1n) - n))));
        }
    }
    return total;
}

export function has3(a, b, c, color) {
    return a === color || b === color || c === color;
}

export function corner_piece(a, b, c) {
    let w = has3(a, b, c, 1n);
    let y = has3(a, b, c, 2n);
    let r = has3(a, b, c, 3n);
    let o = has3(a, b, c, 4n);
    let bl = has3(a, b, c, 5n);
    let g = has3(a, b, c, 6n);
    if (w && g && r) {
        return 0n;
    }
    if (w && g && o) {
        return 1n;
    }
    if (w && bl && o) {
        return 2n;
    }
    if (w && bl && r) {
        return 3n;
    }
    if (y && g && r) {
        return 4n;
    }
    if (y && g && o) {
        return 5n;
    }
    if (y && bl && o) {
        return 6n;
    }
    if (y && bl && r) {
        return 7n;
    }
    _rt.sudo_assert(false, 461);
    return 0n;
}

export function edge_piece(a, b) {
    if (a === 3n && b === 1n || a === 1n && b === 3n) {
        return 0n;
    }
    if (a === 6n && b === 1n || a === 1n && b === 6n) {
        return 1n;
    }
    if (a === 4n && b === 1n || a === 1n && b === 4n) {
        return 2n;
    }
    if (a === 5n && b === 1n || a === 1n && b === 5n) {
        return 3n;
    }
    if (a === 3n && b === 2n || a === 2n && b === 3n) {
        return 4n;
    }
    if (a === 6n && b === 2n || a === 2n && b === 6n) {
        return 5n;
    }
    if (a === 4n && b === 2n || a === 2n && b === 4n) {
        return 6n;
    }
    if (a === 5n && b === 2n || a === 2n && b === 5n) {
        return 7n;
    }
    if (a === 6n && b === 3n || a === 3n && b === 6n) {
        return 8n;
    }
    if (a === 6n && b === 4n || a === 4n && b === 6n) {
        return 9n;
    }
    if (a === 5n && b === 4n || a === 4n && b === 5n) {
        return 10n;
    }
    if (a === 5n && b === 3n || a === 3n && b === 5n) {
        return 11n;
    }
    _rt.sudo_assert(false, 489);
    return 0n;
}

export function is_ud(color) {
    return color === 1n || color === 2n;
}

export function edge_bit(color) {
    let keep = color === 1n || color === 2n || color === 3n || color === 4n;
    if (keep) {
        return 0n;
    }
    return 1n;
}

export function digest_bytes(s3, ori) {
    let buf = _rt.filled(12n, 0n);
    let v = s3;
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = 11n;
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            _rt.put(buf, i, _rt.mod_i64(v, 256n));
            v = _rt.div(v, 256n);
        }
    }
    {
        const _sudo_from_bit = 1n;
        const _sudo_to_bit = 11n;
        for (let bit = _sudo_from_bit; bit <= _sudo_to_bit; bit += 1n) {
            let carry = 0n;
            {
                const _sudo_from_j = 0n;
                const _sudo_to_j = 11n;
                for (let j = _sudo_from_j; j <= _sudo_to_j; j += 1n) {
                    let cur = _rt.chk(_rt.chk(_rt.at(buf, j) * 2n) + carry);
                    _rt.put(buf, j, _rt.mod_i64(cur, 256n));
                    carry = _rt.div(cur, 256n);
                }
            }
            _rt.sudo_assert_eq(carry, 0n, 514);
        }
    }
    let rest = ori;
    {
        const _sudo_from_j = 0n;
        const _sudo_to_j = 11n;
        for (let j = _sudo_from_j; j <= _sudo_to_j; j += 1n) {
            let cur = _rt.chk(_rt.at(buf, j) + _rt.mod_i64(rest, 256n));
            _rt.put(buf, j, _rt.mod_i64(cur, 256n));
            rest = _rt.chk(_rt.div(rest, 256n) + _rt.div(cur, 256n));
        }
    }
    _rt.sudo_assert(_rt.at(buf, 9n) === 0n && _rt.at(buf, 10n) === 0n && _rt.at(buf, 11n) === 0n, 520);
    let out = _rt.lst([]);
    {
        const _sudo_from_j = 8n;
        const _sudo_to_j = 0n;
        for (let j = _sudo_from_j; j >= _sudo_to_j; j -= 1n) {
            out.push(_rt.at(buf, j));
        }
    }
    return _rt.dup(out);
}

export function index_bytes(cube) {
    let perm = _rt.lst([]);
    let ori_acc = 0n;
    let pow3 = 1n;
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = 7n;
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            let c = _rt.dup(_rt.at(cube, cubie_at(cube, _rt.at(cx, i), _rt.at(cy, i), _rt.at(cz, i))));
            let a0 = sticker_on(c, _rt.at(cax, _rt.chk(i * 3n)));
            let a1 = sticker_on(c, _rt.at(cax, _rt.chk(_rt.chk(i * 3n) + 1n)));
            let a2 = sticker_on(c, _rt.at(cax, _rt.chk(_rt.chk(i * 3n) + 2n)));
            perm.push(corner_piece(a0, a1, a2));
            let slot = 0n;
            if (is_ud(a1)) {
                slot = 1n;
            }
            if (is_ud(a2)) {
                slot = 2n;
            }
            if (i < 7n) {
                ori_acc = _rt.chk(ori_acc + _rt.chk(slot * pow3));
                pow3 = _rt.chk(pow3 * 3n);
            }
        }
    }
    let s3 = _rt.chk(_rt.chk(rank_perm(perm) * 2187n) + ori_acc);
    let eperm = _rt.lst([]);
    let eori = 0n;
    let bit = 1n;
    {
        const _sudo_from_i = 0n;
        const _sudo_to_i = 11n;
        for (let i = _sudo_from_i; i <= _sudo_to_i; i += 1n) {
            let c = _rt.dup(_rt.at(cube, cubie_at(cube, _rt.at(ex, i), _rt.at(ey, i), _rt.at(ez, i))));
            let a0 = sticker_on(c, _rt.at(eax, _rt.chk(i * 2n)));
            let a1 = sticker_on(c, _rt.at(eax, _rt.chk(_rt.chk(i * 2n) + 1n)));
            eperm.push(edge_piece(a0, a1));
            if (i < 11n) {
                eori = _rt.chk(eori + _rt.chk(edge_bit(a0) * bit));
                bit = _rt.chk(bit * 2n);
            }
        }
    }
    s3 = _rt.chk(_rt.chk(s3 * 239500800n) + _rt.div(rank_perm(eperm), 2n));
    return digest_bytes(s3, eori);
}

export function finish(s) {
    s.cube = apply_turns(s.cube, 4n, 2n);
    s = push_step(s, _rt.txt("closer"), _rt.txt("F2"), blank(), 0n, 0n, blank(), blank());
    s.cube = apply_turns(s.cube, 5n, 2n);
    s = push_step(s, _rt.txt("closer"), _rt.txt("B2"), blank(), 0n, 0n, blank(), blank());
    s.cube = reorient(s.cube, 1n, 6n);
    s = push_step(s, _rt.txt("canonicalize"), blank(), blank(), 0n, 0n, blank(), blank());
    s.done = true;
    return s;
}

export function evaluate(s) {
    _rt.sudo_assert(!s.done, 569);
    let padded = pad_tape(nybbles_of(s.message), s.version);
    if (s.version === 1n) {
        let i = s.processed;
        let nleft = _rt.div(_rt.chk(globalThis.BigInt(padded.length) - i), 8n);
        {
            const _sudo_from_b = 0n;
            const _sudo_to_b = _rt.chk(nleft - 1n);
            for (let b = _sudo_from_b; b <= _sudo_to_b; b += 1n) {
                s = apply_v1_block(s, padded, _rt.div(i, 8n));
                i = _rt.chk(i + 8n);
            }
        }
    } else {
        let i = s.processed;
        let nleft = _rt.chk(globalThis.BigInt(padded.length) - i);
        {
            const _sudo_from_k = 0n;
            const _sudo_to_k = _rt.chk(nleft - 1n);
            for (let k = _sudo_from_k; k <= _sudo_to_k; k += 1n) {
                s = apply_v2_symbol(s, _rt.at(padded, i), i);
                i = _rt.chk(i + 1n);
            }
        }
    }
    s = finish(s);
    return [_rt.rec(new Evaluation(index_bytes(s.cube), _rt.dup(s.steps))), s];
}

export function run(version, message) {
    let s = fresh(version);
    s = update(s, message);
    let _sudo_h0;
    [_sudo_h0, s] = evaluate(s);
    return _rt.dup(_sudo_h0);
}

export function check(version, message, digest, faces, nsteps) {
    let got = run(version, message);
    _rt.sudo_assert_eq(got.digest, digest, 593);
    _rt.sudo_assert_eq(globalThis.BigInt(got.trace.length), nsteps, 594);
    _rt.sudo_assert_eq(_rt.at(got.trace, _rt.chk(nsteps - 1n)).facelets, faces, 595);
    _rt.sudo_assert_eq(_rt.at(got.trace, 0n).kind, _rt.txt("move"), 596);
    _rt.sudo_assert_eq(_rt.at(got.trace, _rt.chk(nsteps - 1n)).kind, _rt.txt("canonicalize"), 597);
}

