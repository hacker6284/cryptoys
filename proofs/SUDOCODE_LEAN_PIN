# sudocode Lean backend pin.
# Residual merge risk: this is an unmerged draft (sudocode PR #5),
# not a commit on main. Main does not ship backends/lean/ (not in
# ALL_BACKENDS). Branch cursor/lean-external-backend-36b2, includes
# the _fs Flow-binder shadow fix. Bump only when you intend to
# change the emitter, then re-run proofs/emit_lean.sh.
df152a2dcd33c6c8652a228e476aae5868333330
