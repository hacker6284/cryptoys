-- DO NOT EDIT. Generated from doubledeal_cbc_hmac.sudo by tools/emit_lean.py.
-- Algorithm source of truth is the .sudo file. Regenerate with
--   proofs/emit_lean.sh
-- This is not a sudo↔Lean semantic-equivalence theorem.
-- Generated by sudoc lean backend from doubledeal_cbc_hmac.sudo
import SudoRt
import Megadreifach
set_option linter.unusedVariables false

namespace Doubledeal_cbc_hmac

def hmac_block : Int := (28 : Int)

def digest_len : Int := (29 : Int)

def cipher_block : Int := (29 : Int)

def ipad_byte : Int := (54 : Int)

def opad_byte : Int := (92 : Int)

def version_label : Array (Int) := (#[68, 111, 117, 98, 108, 101, 68, 101, 97, 108, 45, 67, 66, 67, 45, 72, 77, 65, 67, 47, 118, 49] : Array Int)

def enc_label : Array (Int) := (#[68, 111, 117, 98, 108, 101, 68, 101, 97, 108, 45, 67, 66, 67, 45, 72, 77, 65, 67, 47, 118, 49, 47, 101, 110, 99] : Array Int)

def mac_label : Array (Int) := (#[68, 111, 117, 98, 108, 101, 68, 101, 97, 108, 45, 67, 66, 67, 45, 72, 77, 65, 67, 47, 118, 49, 47, 109, 97, 99] : Array Int)

def require_bytes (xs : Array (Int)) : Except SudoRt.Trap (Unit) :=
  do
    let _t9 ← SudoRt.subI (SudoRt.listLen xs) (1 : Int)
    let _fromV := (0 : Int)
    let _toV := _t9
    let fuel : Nat := if _fromV > _toV then 1 else (_toV - _fromV).natAbs + 1
    let _init10 := _fromV
    let _out ← (SudoRt.runLoopOn (ρ := Unit) _init10 fuel (fun σ =>
    let i := σ
    do
      if i > _toV then
        pure (SudoRt.Flow.brk (ρ := Unit) i)
      else
        match ← ((do
  let _t2 ← SudoRt.atL xs i
  let _t4 ← (if (decide (_t2 ≥ (0 : Int))) then (do
  let _t5 ← SudoRt.atL xs i
  pure (decide (_t5 ≤ (255 : Int)))) else pure false)
  let _as7 ← SudoRt.sudoAssert _t4 20
  pure (SudoRt.Flow.cont (ρ := Unit) ())) : Except SudoRt.Trap (SudoRt.Flow _ (Unit))) with
        | .ret r => pure (SudoRt.Flow.ret (ρ := Unit) r)
        | .brk _fs => pure (SudoRt.Flow.brk (ρ := Unit) i)
        | .cont _fs => do
            if i == _toV then
              pure (SudoRt.Flow.brk (ρ := Unit) i)
            else do
              let i' ← SudoRt.addI i (1 : Int)
              pure (SudoRt.Flow.cont (ρ := Unit) i')) (fun σ =>
    do
      pure ()) (fun r => pure r))
    pure _out

def xor_byte (a : Int) (b : Int) : Except SudoRt.Trap (Int) :=
  do
    let _t12 ← (if (decide (a ≥ (0 : Int))) then (do
  pure (decide (a ≤ (255 : Int)))) else pure false)
    let _as14 ← SudoRt.sudoAssert _t12 23
    let _t16 ← (if (decide (b ≥ (0 : Int))) then (do
  pure (decide (b ≤ (255 : Int)))) else pure false)
    let _as18 ← SudoRt.sudoAssert _t16 24
    let out := (0 : Int)
    let place := (1 : Int)
    let left := a
    let right := b
    let _fromV := (0 : Int)
    let _toV := (7 : Int)
    let fuel : Nat := if _fromV > _toV then 1 else (_toV - _fromV).natAbs + 1
    let _init36 := (_fromV, (out, left, right, place))
    let _out ← (SudoRt.runLoopOn (ρ := Int) _init36 fuel (fun σ =>
    let bit := σ.1
    let out := σ.2.1
    let _sp30 := σ.2.2
    let left := _sp30.1
    let _sp31 := _sp30.2
    let right := _sp31.1
    let _sp32 := _sp31.2
    let place := _sp32
    do
      if bit > _toV then
        pure (SudoRt.Flow.brk (ρ := Int) (bit, (out, left, right, place)))
      else
        match ← ((do
  let _t20 ← SudoRt.modI left (2 : Int)
  let la := _t20
  let _t21 ← SudoRt.modI right (2 : Int)
  let rb := _t21
  if (!(SudoRt.SEq.beq la rb)) then
    do
      let _t23 ← SudoRt.addI out place
      let out := _t23
      let _t24 ← SudoRt.divI left (2 : Int)
      let left := _t24
      let _t25 ← SudoRt.divI right (2 : Int)
      let right := _t25
      let _t26 ← SudoRt.mulI place (2 : Int)
      let place := _t26
      pure (SudoRt.Flow.cont (ρ := Int) (out, left, right, place))
  else
    do
      let _t27 ← SudoRt.divI left (2 : Int)
      let left := _t27
      let _t28 ← SudoRt.divI right (2 : Int)
      let right := _t28
      let _t29 ← SudoRt.mulI place (2 : Int)
      let place := _t29
      pure (SudoRt.Flow.cont (ρ := Int) (out, left, right, place))) : Except SudoRt.Trap (SudoRt.Flow _ (Int))) with
        | .ret r => pure (SudoRt.Flow.ret (ρ := Int) r)
        | .brk _fs => pure (SudoRt.Flow.brk (ρ := Int) (bit, _fs))
        | .cont _fs => do
            if bit == _toV then
              pure (SudoRt.Flow.brk (ρ := Int) (bit, _fs))
            else do
              let i' ← SudoRt.addI bit (1 : Int)
              pure (SudoRt.Flow.cont (ρ := Int) (i', _fs))) (fun σ =>
    let out := σ.2.1
    let _sp33 := σ.2.2
    let left := _sp33.1
    let _sp34 := _sp33.2
    let right := _sp34.1
    let _sp35 := _sp34.2
    let place := _sp35
    do
      pure out) (fun r => pure r))
    pure _out

def xor_bytes (a : Array (Int)) (b : Array (Int)) : Except SudoRt.Trap (Array (Int)) :=
  do
    let _as40 ← SudoRt.sudoAssertEq (SudoRt.listLen a) (SudoRt.listLen b) 40
    let _t41 ← require_bytes a
    let _u42 := _t41
    let _t43 ← require_bytes b
    let _u44 := _t43
    let out := (#[] : Array (Int))
    let _t53 ← SudoRt.subI (SudoRt.listLen a) (1 : Int)
    let _fromV := (0 : Int)
    let _toV := _t53
    let fuel : Nat := if _fromV > _toV then 1 else (_toV - _fromV).natAbs + 1
    let _init54 := (_fromV, out)
    let _out ← (SudoRt.runLoopOn (ρ := Array (Int)) _init54 fuel (fun σ =>
    let i := σ.1
    let out := σ.2
    do
      if i > _toV then
        pure (SudoRt.Flow.brk (ρ := Array (Int)) (i, out))
      else
        match ← ((do
  let _t46 ← SudoRt.atL a i
  let _t47 ← SudoRt.atL b i
  let _t48 ← xor_byte _t46 _t47
  let _mb49 := SudoRt.appendL out _t48
  let ⟨_nr50, _⟩ := _mb49
  let out := _nr50
  let _hm37 := ()
  let _u51 := _hm37
  pure (SudoRt.Flow.cont (ρ := Array (Int)) out)) : Except SudoRt.Trap (SudoRt.Flow _ (Array (Int)))) with
        | .ret r => pure (SudoRt.Flow.ret (ρ := Array (Int)) r)
        | .brk _fs => pure (SudoRt.Flow.brk (ρ := Array (Int)) (i, _fs))
        | .cont _fs => do
            if i == _toV then
              pure (SudoRt.Flow.brk (ρ := Array (Int)) (i, _fs))
            else do
              let i' ← SudoRt.addI i (1 : Int)
              pure (SudoRt.Flow.cont (ρ := Array (Int)) (i', _fs))) (fun σ =>
    let out := σ.2
    do
      pure out) (fun r => pure r))
    pure _out

def u16be (n : Int) : Except SudoRt.Trap (Array (Int)) :=
  do
    let _t58 ← (if (decide (n ≥ (0 : Int))) then (do
  pure (decide (n ≤ (65535 : Int)))) else pure false)
    let _as60 ← SudoRt.sudoAssert _t58 49
    let out := (#[] : Array (Int))
    let _t61 ← SudoRt.divI n (256 : Int)
    let _mb62 := SudoRt.appendL out _t61
    let ⟨_nr63, _⟩ := _mb62
    let out := _nr63
    let _hm55 := ()
    let _u64 := _hm55
    let _t65 ← SudoRt.modI n (256 : Int)
    let _mb66 := SudoRt.appendL out _t65
    let ⟨_nr67, _⟩ := _mb66
    let out := _nr67
    let _hm56 := ()
    let _u68 := _hm56
    pure out

def u64be (n : Int) : Except SudoRt.Trap (Array (Int)) :=
  do
    let _as72 ← SudoRt.sudoAssert (decide (n ≥ (0 : Int))) 56
    let parts := (#[] : Array (Int))
    let rest := n
    let _fromV := (1 : Int)
    let _toV := (8 : Int)
    let fuel : Nat := if _fromV > _toV then 1 else (_toV - _fromV).natAbs + 1
    let _init88 := (_fromV, (parts, rest))
    let _out ← (SudoRt.runLoopOn (ρ := Array (Int)) _init88 fuel (fun σ =>
    let i := σ.1
    let parts := σ.2.1
    let _sp86 := σ.2.2
    let rest := _sp86
    do
      if i > _toV then
        pure (SudoRt.Flow.brk (ρ := Array (Int)) (i, (parts, rest)))
      else
        match ← ((do
  let _t74 ← SudoRt.modI rest (256 : Int)
  let _mb75 := SudoRt.appendL parts _t74
  let ⟨_nr76, _⟩ := _mb75
  let parts := _nr76
  let _hm69 := ()
  let _u77 := _hm69
  let _t78 ← SudoRt.divI rest (256 : Int)
  let rest := _t78
  pure (SudoRt.Flow.cont (ρ := Array (Int)) (parts, rest))) : Except SudoRt.Trap (SudoRt.Flow _ (Array (Int)))) with
        | .ret r => pure (SudoRt.Flow.ret (ρ := Array (Int)) r)
        | .brk _fs => pure (SudoRt.Flow.brk (ρ := Array (Int)) (i, _fs))
        | .cont _fs => do
            if i == _toV then
              pure (SudoRt.Flow.brk (ρ := Array (Int)) (i, _fs))
            else do
              let i' ← SudoRt.addI i (1 : Int)
              pure (SudoRt.Flow.cont (ρ := Array (Int)) (i', _fs))) (fun σ =>
    let parts := σ.2.1
    let _sp87 := σ.2.2
    let rest := _sp87
    do
      let _as79 ← SudoRt.sudoAssertEq rest (0 : Int) 62
      let out := (#[] : Array (Int))
      let _fromV := (7 : Int)
      let _toV := (0 : Int)
      let fuel : Nat := if _fromV < _toV then 1 else (_fromV - _toV).natAbs + 1
      let _init85 := (_fromV, out)
      let _out ← (SudoRt.runLoopOn (ρ := Array (Int)) _init85 fuel (fun σ =>
    let i := σ.1
    let out := σ.2
    do
      if i < _toV then
        pure (SudoRt.Flow.brk (ρ := Array (Int)) (i, out))
      else
        match ← ((do
  let _t81 ← SudoRt.atL parts i
  let _mb82 := SudoRt.appendL out _t81
  let ⟨_nr83, _⟩ := _mb82
  let out := _nr83
  let _hm70 := ()
  let _u84 := _hm70
  pure (SudoRt.Flow.cont (ρ := Array (Int)) out)) : Except SudoRt.Trap (SudoRt.Flow _ (Array (Int)))) with
        | .ret r => pure (SudoRt.Flow.ret (ρ := Array (Int)) r)
        | .brk _fs => pure (SudoRt.Flow.brk (ρ := Array (Int)) (i, _fs))
        | .cont _fs => do
            if i == _toV then
              pure (SudoRt.Flow.brk (ρ := Array (Int)) (i, _fs))
            else do
              let i' ← SudoRt.subI i (1 : Int)
              pure (SudoRt.Flow.cont (ρ := Array (Int)) (i', _fs))) (fun σ =>
    let out := σ.2
    do
      pure out) (fun r => pure r))
      pure _out) (fun r => pure r))
    pure _out

def take_prefix (xs : Array (Int)) (n : Int) : Except SudoRt.Trap (Array (Int)) :=
  do
    let _as91 ← SudoRt.sudoAssert (decide (n ≥ (0 : Int))) 69
    let _as94 ← SudoRt.sudoAssert (decide ((SudoRt.listLen xs) ≥ n)) 70
    let out := (#[] : Array (Int))
    let _t100 ← SudoRt.subI n (1 : Int)
    let _fromV := (0 : Int)
    let _toV := _t100
    let fuel : Nat := if _fromV > _toV then 1 else (_toV - _fromV).natAbs + 1
    let _init101 := (_fromV, out)
    let _out ← (SudoRt.runLoopOn (ρ := Array (Int)) _init101 fuel (fun σ =>
    let i := σ.1
    let out := σ.2
    do
      if i > _toV then
        pure (SudoRt.Flow.brk (ρ := Array (Int)) (i, out))
      else
        match ← ((do
  let _t96 ← SudoRt.atL xs i
  let _mb97 := SudoRt.appendL out _t96
  let ⟨_nr98, _⟩ := _mb97
  let out := _nr98
  let _hm89 := ()
  let _u99 := _hm89
  pure (SudoRt.Flow.cont (ρ := Array (Int)) out)) : Except SudoRt.Trap (SudoRt.Flow _ (Array (Int)))) with
        | .ret r => pure (SudoRt.Flow.ret (ρ := Array (Int)) r)
        | .brk _fs => pure (SudoRt.Flow.brk (ρ := Array (Int)) (i, _fs))
        | .cont _fs => do
            if i == _toV then
              pure (SudoRt.Flow.brk (ρ := Array (Int)) (i, _fs))
            else do
              let i' ← SudoRt.addI i (1 : Int)
              pure (SudoRt.Flow.cont (ρ := Array (Int)) (i', _fs))) (fun σ =>
    let out := σ.2
    do
      pure out) (fun r => pure r))
    pure _out

def pad_zeros (xs : Array (Int)) (n : Int) : Except SudoRt.Trap (Array (Int)) :=
  do
    let _as106 ← SudoRt.sudoAssert (decide (n ≥ (SudoRt.listLen xs))) 77
    let out := (#[] : Array (Int))
    let _t117 ← SudoRt.subI n (1 : Int)
    let _fromV := (0 : Int)
    let _toV := _t117
    let fuel : Nat := if _fromV > _toV then 1 else (_toV - _fromV).natAbs + 1
    let _init118 := (_fromV, out)
    let _out ← (SudoRt.runLoopOn (ρ := Array (Int)) _init118 fuel (fun σ =>
    let i := σ.1
    let out := σ.2
    do
      if i > _toV then
        pure (SudoRt.Flow.brk (ρ := Array (Int)) (i, out))
      else
        match ← ((do
  if (decide (i < (SudoRt.listLen xs))) then
    do
      let _t110 ← SudoRt.atL xs i
      let _mb111 := SudoRt.appendL out _t110
      let ⟨_nr112, _⟩ := _mb111
      let out := _nr112
      let _hm102 := ()
      let _u113 := _hm102
      pure (SudoRt.Flow.cont (ρ := Array (Int)) out)
  else
    do
      let _mb114 := SudoRt.appendL out (0 : Int)
      let ⟨_nr115, _⟩ := _mb114
      let out := _nr115
      let _hm103 := ()
      let _u116 := _hm103
      pure (SudoRt.Flow.cont (ρ := Array (Int)) out)) : Except SudoRt.Trap (SudoRt.Flow _ (Array (Int)))) with
        | .ret r => pure (SudoRt.Flow.ret (ρ := Array (Int)) r)
        | .brk _fs => pure (SudoRt.Flow.brk (ρ := Array (Int)) (i, _fs))
        | .cont _fs => do
            if i == _toV then
              pure (SudoRt.Flow.brk (ρ := Array (Int)) (i, _fs))
            else do
              let i' ← SudoRt.addI i (1 : Int)
              pure (SudoRt.Flow.cont (ρ := Array (Int)) (i', _fs))) (fun σ =>
    let out := σ.2
    do
      pure out) (fun r => pure r))
    pure _out

def hmac_normalize_key (key : Array (Int)) : Except SudoRt.Trap (Array (Int)) :=
  do
    let _t119 ← require_bytes key
    let _u120 := _t119
    let k := key
    if (decide ((SudoRt.listLen k) > hmac_block)) then
      do
        let _t123 ← Megadreifach.v_Hash k
        let k := _t123
        if (decide ((SudoRt.listLen k) > hmac_block)) then
          do
            let _t126 ← take_prefix k hmac_block
            let k := _t126
            let _t127 ← pad_zeros k hmac_block
            pure _t127
        else
          do
            let _t128 ← pad_zeros k hmac_block
            pure _t128
    else
      do
        if (decide ((SudoRt.listLen k) > hmac_block)) then
          do
            let _t131 ← take_prefix k hmac_block
            let k := _t131
            let _t132 ← pad_zeros k hmac_block
            pure _t132
        else
          do
            let _t133 ← pad_zeros k hmac_block
            pure _t133

def xor_pad (key : Array (Int)) (pad : Int) : Except SudoRt.Trap (Array (Int)) :=
  do
    let _as136 ← SudoRt.sudoAssertEq (SudoRt.listLen key) hmac_block 96
    let out := (#[] : Array (Int))
    let _t143 ← SudoRt.subI hmac_block (1 : Int)
    let _fromV := (0 : Int)
    let _toV := _t143
    let fuel : Nat := if _fromV > _toV then 1 else (_toV - _fromV).natAbs + 1
    let _init144 := (_fromV, out)
    let _out ← (SudoRt.runLoopOn (ρ := Array (Int)) _init144 fuel (fun σ =>
    let i := σ.1
    let out := σ.2
    do
      if i > _toV then
        pure (SudoRt.Flow.brk (ρ := Array (Int)) (i, out))
      else
        match ← ((do
  let _t138 ← SudoRt.atL key i
  let _t139 ← xor_byte _t138 pad
  let _mb140 := SudoRt.appendL out _t139
  let ⟨_nr141, _⟩ := _mb140
  let out := _nr141
  let _hm134 := ()
  let _u142 := _hm134
  pure (SudoRt.Flow.cont (ρ := Array (Int)) out)) : Except SudoRt.Trap (SudoRt.Flow _ (Array (Int)))) with
        | .ret r => pure (SudoRt.Flow.ret (ρ := Array (Int)) r)
        | .brk _fs => pure (SudoRt.Flow.brk (ρ := Array (Int)) (i, _fs))
        | .cont _fs => do
            if i == _toV then
              pure (SudoRt.Flow.brk (ρ := Array (Int)) (i, _fs))
            else do
              let i' ← SudoRt.addI i (1 : Int)
              pure (SudoRt.Flow.cont (ρ := Array (Int)) (i', _fs))) (fun σ =>
    let out := σ.2
    do
      pure out) (fun r => pure r))
    pure _out

def v_HMAC (key : Array (Int)) (msg : Array (Int)) : Except SudoRt.Trap (Array (Int)) :=
  do
    let _t145 ← require_bytes msg
    let _u146 := _t145
    let _t147 ← hmac_normalize_key key
    let k := _t147
    let _t148 ← xor_pad k ipad_byte
    let _t150 ← Megadreifach.v_Hash (SudoRt.concatL _t148 msg)
    let inner := _t150
    let _t151 ← xor_pad k opad_byte
    let _t153 ← Megadreifach.v_Hash (SudoRt.concatL _t151 inner)
    let tag := _t153
    let _as155 ← SudoRt.sudoAssertEq (SudoRt.listLen tag) digest_len 107
    pure tag

def v_HMAC_MegaDreifach (key : Array (Int)) (msg : Array (Int)) : Except SudoRt.Trap (Array (Int)) :=
  do
    let _t156 ← v_HMAC key msg
    pure _t156

def pad_iso7816 (msg : Array (Int)) : Except SudoRt.Trap (Array (Int)) :=
  do
    let _t158 ← require_bytes msg
    let _u159 := _t158
    let out := (SudoRt.concatL msg (#[(128 : Int)] : Array (Int)))
    let _t162 ← SudoRt.modI (SudoRt.listLen out) hmac_block
    let _t163 ← SudoRt.subI hmac_block _t162
    let _t164 ← SudoRt.modI _t163 hmac_block
    let nzero := _t164
    let _fromV := (1 : Int)
    let _toV := nzero
    let fuel : Nat := if _fromV > _toV then 1 else (_toV - _fromV).natAbs + 1
    let _init172 := (_fromV, out)
    let _out ← (SudoRt.runLoopOn (ρ := Array (Int)) _init172 fuel (fun σ =>
    let i := σ.1
    let out := σ.2
    do
      if i > _toV then
        pure (SudoRt.Flow.brk (ρ := Array (Int)) (i, out))
      else
        match ← ((do
  let _mb166 := SudoRt.appendL out (0 : Int)
  let ⟨_nr167, _⟩ := _mb166
  let out := _nr167
  let _hm157 := ()
  let _u168 := _hm157
  pure (SudoRt.Flow.cont (ρ := Array (Int)) out)) : Except SudoRt.Trap (SudoRt.Flow _ (Array (Int)))) with
        | .ret r => pure (SudoRt.Flow.ret (ρ := Array (Int)) r)
        | .brk _fs => pure (SudoRt.Flow.brk (ρ := Array (Int)) (i, _fs))
        | .cont _fs => do
            if i == _toV then
              pure (SudoRt.Flow.brk (ρ := Array (Int)) (i, _fs))
            else do
              let i' ← SudoRt.addI i (1 : Int)
              pure (SudoRt.Flow.cont (ρ := Array (Int)) (i', _fs))) (fun σ =>
    let out := σ.2
    do
      let _t170 ← SudoRt.modI (SudoRt.listLen out) hmac_block
      let _as171 ← SudoRt.sudoAssertEq _t170 (0 : Int) 119
      pure out) (fun r => pure r))
    pure _out

def unpad_iso7816 (msg : Array (Int)) : Except SudoRt.Trap (Array (Int)) :=
  do
    let _as176 ← SudoRt.sudoAssert (decide ((SudoRt.listLen msg) > (0 : Int))) 123
    let _t178 ← SudoRt.modI (SudoRt.listLen msg) hmac_block
    let _as179 ← SudoRt.sudoAssertEq _t178 (0 : Int) 124
    let _t180 ← require_bytes msg
    let _u181 := _t180
    let _t183 ← SudoRt.subI (SudoRt.listLen msg) (1 : Int)
    let i := _t183
    let found := (0 : Int)
    let _fromV := (1 : Int)
    let _toV := (SudoRt.listLen msg)
    let fuel : Nat := if _fromV > _toV then 1 else (_toV - _fromV).natAbs + 1
    let _init205 := (_fromV, (i, found))
    let _out ← (SudoRt.runLoopOn (ρ := Array (Int)) _init205 fuel (fun σ =>
    let n := σ.1
    let i := σ.2.1
    let _sp203 := σ.2.2
    let found := _sp203
    do
      if n > _toV then
        pure (SudoRt.Flow.brk (ρ := Array (Int)) (n, (i, found)))
      else
        match ← ((do
  if (SudoRt.SEq.beq found (0 : Int)) then
    do
      let _t186 ← SudoRt.atL msg i
      let _t189 ← SudoRt.atL msg i
      if (SudoRt.SEq.beq _t186 (0 : Int)) then
        do
          let _t188 ← SudoRt.subI i (1 : Int)
          let i := _t188
          pure (SudoRt.Flow.cont (ρ := Array (Int)) (i, found))
      else
        do
          if (SudoRt.SEq.beq _t189 (128 : Int)) then
            do
              let found := (1 : Int)
              pure (SudoRt.Flow.cont (ρ := Array (Int)) (i, found))
          else
            do
              let _as191 ← SudoRt.sudoAssert false 135
              pure (SudoRt.Flow.cont (ρ := Array (Int)) (i, found))
  else
    do
      pure (SudoRt.Flow.cont (ρ := Array (Int)) (i, found))) : Except SudoRt.Trap (SudoRt.Flow _ (Array (Int)))) with
        | .ret r => pure (SudoRt.Flow.ret (ρ := Array (Int)) r)
        | .brk _fs => pure (SudoRt.Flow.brk (ρ := Array (Int)) (n, _fs))
        | .cont _fs => do
            if n == _toV then
              pure (SudoRt.Flow.brk (ρ := Array (Int)) (n, _fs))
            else do
              let i' ← SudoRt.addI n (1 : Int)
              pure (SudoRt.Flow.cont (ρ := Array (Int)) (i', _fs))) (fun σ =>
    let i := σ.2.1
    let _sp204 := σ.2.2
    let found := _sp204
    do
      let _as193 ← SudoRt.sudoAssertEq found (1 : Int) 136
      let _as195 ← SudoRt.sudoAssert (decide (i ≥ (0 : Int))) 137
      let out := (#[] : Array (Int))
      let _t201 ← SudoRt.subI i (1 : Int)
      let _fromV := (0 : Int)
      let _toV := _t201
      let fuel : Nat := if _fromV > _toV then 1 else (_toV - _fromV).natAbs + 1
      let _init202 := (_fromV, out)
      let _out ← (SudoRt.runLoopOn (ρ := Array (Int)) _init202 fuel (fun σ =>
    let j := σ.1
    let out := σ.2
    do
      if j > _toV then
        pure (SudoRt.Flow.brk (ρ := Array (Int)) (j, out))
      else
        match ← ((do
  let _t197 ← SudoRt.atL msg j
  let _mb198 := SudoRt.appendL out _t197
  let ⟨_nr199, _⟩ := _mb198
  let out := _nr199
  let _hm173 := ()
  let _u200 := _hm173
  pure (SudoRt.Flow.cont (ρ := Array (Int)) out)) : Except SudoRt.Trap (SudoRt.Flow _ (Array (Int)))) with
        | .ret r => pure (SudoRt.Flow.ret (ρ := Array (Int)) r)
        | .brk _fs => pure (SudoRt.Flow.brk (ρ := Array (Int)) (j, _fs))
        | .cont _fs => do
            if j == _toV then
              pure (SudoRt.Flow.brk (ρ := Array (Int)) (j, _fs))
            else do
              let i' ← SudoRt.addI j (1 : Int)
              pure (SudoRt.Flow.cont (ρ := Array (Int)) (i', _fs))) (fun σ =>
    let out := σ.2
    do
      pure out) (fun r => pure r))
      pure _out) (fun r => pure r))
    pure _out

def mac_input (aad : Array (Int)) (iv : Array (Int)) (ciphertext : Array (Int)) : Except SudoRt.Trap (Array (Int)) :=
  do
    let _t206 ← require_bytes aad
    let _u207 := _t206
    let _t208 ← require_bytes iv
    let _u209 := _t208
    let _t210 ← require_bytes ciphertext
    let _u211 := _t210
    let ver := version_label
    let _t213 ← u16be (SudoRt.listLen ver)
    let _t216 ← u64be (SudoRt.listLen aad)
    let _t220 ← u64be (SudoRt.listLen iv)
    let _t224 ← u64be (SudoRt.listLen ciphertext)
    pure (SudoRt.concatL (SudoRt.concatL (SudoRt.concatL (SudoRt.concatL (SudoRt.concatL (SudoRt.concatL (SudoRt.concatL _t213 ver) _t216) aad) _t220) iv) _t224) ciphertext)

def derive_keys (master : Array (Int)) : Except SudoRt.Trap ((Array (Int)) × (Array (Int))) :=
  do
    let _t227 ← require_bytes master
    let _u228 := _t227
    let _as231 ← SudoRt.sudoAssert (decide ((SudoRt.listLen master) > (0 : Int))) 152
    let _t233 ← u16be (SudoRt.listLen enc_label)
    let _t236 ← u64be (SudoRt.listLen master)
    let _t239 ← Megadreifach.v_Hash (SudoRt.concatL (SudoRt.concatL (SudoRt.concatL _t233 enc_label) _t236) master)
    let enc_digest := _t239
    let _t241 ← u16be (SudoRt.listLen mac_label)
    let _t244 ← u64be (SudoRt.listLen master)
    let _t247 ← Megadreifach.v_Hash (SudoRt.concatL (SudoRt.concatL (SudoRt.concatL _t241 mac_label) _t244) master)
    let mac_digest := _t247
    let _as249 ← SudoRt.sudoAssertEq (SudoRt.listLen enc_digest) digest_len 155
    let _as251 ← SudoRt.sudoAssertEq (SudoRt.listLen mac_digest) digest_len 156
    let _t252 ← take_prefix enc_digest hmac_block
    pure (_t252, mac_digest)

def cbc_chain_from_cipher_block (block : Array (Int)) : Except SudoRt.Trap (Array (Int)) :=
  do
    let _as255 ← SudoRt.sudoAssertEq (SudoRt.listLen block) cipher_block 160
    let _t256 ← require_bytes block
    let _u257 := _t256
    let out := (#[] : Array (Int))
    let _fromV := (1 : Int)
    let _toV := hmac_block
    let fuel : Nat := if _fromV > _toV then 1 else (_toV - _fromV).natAbs + 1
    let _init263 := (_fromV, out)
    let _out ← (SudoRt.runLoopOn (ρ := Array (Int)) _init263 fuel (fun σ =>
    let i := σ.1
    let out := σ.2
    do
      if i > _toV then
        pure (SudoRt.Flow.brk (ρ := Array (Int)) (i, out))
      else
        match ← ((do
  let _t259 ← SudoRt.atL block i
  let _mb260 := SudoRt.appendL out _t259
  let ⟨_nr261, _⟩ := _mb260
  let out := _nr261
  let _hm253 := ()
  let _u262 := _hm253
  pure (SudoRt.Flow.cont (ρ := Array (Int)) out)) : Except SudoRt.Trap (SudoRt.Flow _ (Array (Int)))) with
        | .ret r => pure (SudoRt.Flow.ret (ρ := Array (Int)) r)
        | .brk _fs => pure (SudoRt.Flow.brk (ρ := Array (Int)) (i, _fs))
        | .cont _fs => do
            if i == _toV then
              pure (SudoRt.Flow.brk (ρ := Array (Int)) (i, _fs))
            else do
              let i' ← SudoRt.addI i (1 : Int)
              pure (SudoRt.Flow.cont (ρ := Array (Int)) (i', _fs))) (fun σ =>
    let out := σ.2
    do
      pure out) (fun r => pure r))
    pure _out

def tags_equal (a : Array (Int)) (b : Array (Int)) : Except SudoRt.Trap (Bool) :=
  do
    if (!(SudoRt.SEq.beq (SudoRt.listLen a) (SudoRt.listLen b))) then
      do
        pure false
    else
      do
        let ok := true
        let _t272 ← SudoRt.subI (SudoRt.listLen a) (1 : Int)
        let _fromV := (0 : Int)
        let _toV := _t272
        let fuel : Nat := if _fromV > _toV then 1 else (_toV - _fromV).natAbs + 1
        let _init273 := (_fromV, ok)
        let _out ← (SudoRt.runLoopOn (ρ := Bool) _init273 fuel (fun σ =>
    let i := σ.1
    let ok := σ.2
    do
      if i > _toV then
        pure (SudoRt.Flow.brk (ρ := Bool) (i, ok))
      else
        match ← ((do
  let _t268 ← SudoRt.atL a i
  let _t269 ← SudoRt.atL b i
  if (!(SudoRt.SEq.beq _t268 _t269)) then
    do
      let ok := false
      pure (SudoRt.Flow.cont (ρ := Bool) ok)
  else
    do
      pure (SudoRt.Flow.cont (ρ := Bool) ok)) : Except SudoRt.Trap (SudoRt.Flow _ (Bool))) with
        | .ret r => pure (SudoRt.Flow.ret (ρ := Bool) r)
        | .brk _fs => pure (SudoRt.Flow.brk (ρ := Bool) (i, _fs))
        | .cont _fs => do
            if i == _toV then
              pure (SudoRt.Flow.brk (ρ := Bool) (i, _fs))
            else do
              let i' ← SudoRt.addI i (1 : Int)
              pure (SudoRt.Flow.cont (ρ := Bool) (i', _fs))) (fun σ =>
    let ok := σ.2
    do
      pure ok) (fun r => pure r))
        pure _out

end Doubledeal_cbc_hmac
